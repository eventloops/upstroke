# PR #93 — a clean base merge-in keeps the review: frontier review record

| field | value |
|---|---|
| **Verdict** | **CHANGES_REQUIRED**, three findings, recorded as written. One rejected (its immutability objection contradicts the decisions folder's own contract), two repaired in the one documentation commit above the reviewed head, disclosed below; none meets the serious-P1 bar |
| **Reviewed SHA** | `3201012f2e93c0b9e8be367ec335412b526d27c4` |
| Pull request | eventloops/upstroke#93, into `master` |
| Reviewer | `gpt-5.6-sol`, `model_reasoning_effort=max`, read-only sandbox |
| Transport | `codex exec` via `~/bin/review-pr.sh 93`, 90-minute per-pass limit |
| Inputs | the 215-line exact-head diff (8 files) assembled from the PR's own base `e04d83b`, and the body |
| CI at the reviewed SHA | 11/11 success, uncancelled: `upstroke-ci` run 33553640718 (10 jobs) and `upstroke-pr-policy` run 33553640725 |
| Pass history | pass 1 `3201012f`, three findings (this record): the pull request's one full pass under `decisions/2026-09-01-review-effort-rescoped.md` |

The first pull request reviewed under the re-scoped rule, and it says so. One
full pass ran; its findings were triaged per `MAINTAINING.md` step 6 by the
owner's agent under the owner's standing direction of 2026-09-01, and none
reaches the serious-P1 bar: the controlling text (step 6 and the record)
already carried the conditions the summaries omitted, and the hash-recipe
failure needs both diff commands to fail identically. No second pass ran.
The two repairs sit in one documentation-only commit above the reviewed head;
the delta is listed in the body for the owner's read before merge, and the
owner's merge attests it. The rejected finding's row shows why the claim does
not hold.

## The verdict, verbatim

Blockquoted byte-for-byte from the reviewer's saved output
(`~/review-pr93-3201012.md` on the review box, sha256 `34573902dbfab477…`).

> 1. **MUST — Immutable decision records are modified.** The patch appends text to both [2026-08-20 record](/tmp/tmp.iq9jfpQcBF/pr.diff:78) and [2026-09-01 record](/tmp/tmp.iq9jfpQcBF/pr.diff:170). Calling these additions “dated forward notices” does not preserve immutability. The new successor record and mutable index can point backward; landed records themselves must remain byte-for-byte unchanged.
>
> 2. **P1 — The merge checklist omits required safeguards.** The authoritative new rule requires CI green on the merged head and forbids candidates touching workflows, gate scripts, or validators. The [template exemption](/tmp/tmp.iq9jfpQcBF/pr.diff:5) requires only a conflict-free merge and unchanged hash; its CI checkbox concerns CI *before review*. `CLAUDE.md` also omits the prohibited-surface condition, while `CONTRIBUTING.md` omits both conditions.
>
>    Concrete failure: review head `R` edits product code and the CI workflow; later `master` changes an API that code relies on. Merging into `R` is conflict-free and leaves the PR diff identical. The candidate-controlled workflow reports green. The template says the review remains valid, although the canonical rule explicitly excludes this candidate; the unreviewed semantic interaction can land.
>
> 3. **P1 — The asserted “fails closed” hash recipe fails open on errors.** The record prescribes [`git diff … | sha256sum`](/tmp/tmp.iq9jfpQcBF/pr.diff:105), then claims one drifted hunk makes the rule fail closed. In a default POSIX shell, an unavailable or mistyped revision makes `git diff` exit nonzero with no stdout, while `sha256sum` successfully emits the empty-input hash. If both comparisons fail—readily possible in a shallow/incomplete checkout—the hashes match despite an unverified or changed diff. The procedure also never requires trusted-side recomputation. It must require successful diff commands, verified revisions/ancestry, and explicit evidence fields in the template.
>
> The stated eight-path scope otherwise matches the patch, and there is no Rust, panic, `anyhow`, or path-handling surface.
>
> VERDICT: CHANGES_REQUIRED
## Disposition of the three findings

1. Rejected. `decisions/README.md` states the folder's contract: records are
   immutable once landed, and corrections and follow-ups are dated sections
   appended to their own record, never silent edits; a record whose
   conclusion is later overturned says so and links forward. The two notices
   are exactly that, each record's own text is unchanged above them, and the
   2026-08-20 record's rule that widening the exempt set takes a successor
   record is followed by this change. Ledger row
   `PR93-NOTICES-CALLED-IMMUTABILITY-BREACH`, rejected.
2. Fixed. The template's exempt-only parenthetical now names all four
   conditions and points at a new evidence field; CLAUDE.md carries the
   no-gate condition and the recomputation rule; CONTRIBUTING.md carries CI
   green and no gate. Ledger row `PR93-TEMPLATE-EXEMPTION-OMITS-CONDITIONS`,
   fixed.
3. Fixed. The record, step 6 and the template now require each `git diff`
   to succeed, never accept the empty-input hash, verify the reviewed SHA is
   an ancestor of the merged head and the merge commit's second parent is
   the new base tip, and have the owner recompute both hashes from a
   default-branch checkout with the evidence recorded in the template field.
   Ledger row `PR93-HASH-RECIPE-FAILS-OPEN`, fixed.
