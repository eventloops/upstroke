<!-- See also: 2026-08-09-step-6-review.md -->

# Review: Steps 1–4 (whole codebase, gates excluded)

- **Date:** 2026-08-09
- **Scope:** everything at HEAD (`a21ee2f`) except `src/gates.rs` (reviewed 2026-08-08). Covers IR, plan parser, catalog, routing, config (non-gate), validate, Claude adapter, subprocess supervisor, workspace/git, engine, ulid, util, main.
- **Level:** max — 10 finder angles → verification (several findings verified empirically against the real `claude` v2.1.220 CLI and against cmd.exe/git behavior on this machine) → gap sweep.
- **Status:** **all fixed** in the follow-up commit ("Steps 1–4.1"), including every item in the excluded set below. Suite grew 97 → 125 tests; fmt/clippy `-D warnings` clean.

## Reported (top 15, correctness, ranked)

| # | File | Finding | Verdict |
|---|------|---------|---------|
| 1 | agent/claude.rs | `.cmd` shim invoked via `cmd /C` breaks when the shim path has spaces and a second quoted arg (`--settings`) is present — probe passes, every task fails | CONFIRMED (empirical) |
| 2 | agent/claude.rs | Successful run misclassified as `RateLimited` when result/stderr text contains "429"/"rate limit"/"overloaded" — work rolled back, run halted | CONFIRMED |
| 3 | agent/claude.rs | Empty PATH entry (trailing `;`) → relative `claude.cmd` resolved against CWD → repo-controlled code executed at probe | CONFIRMED (empirical) |
| 4 | agent/claude.rs | `--settings` merges additively with user/project/local settings (allow rules union) and invalid settings are silently ignored in `-p` mode → the §20 narrow sandbox fails open | CONFIRMED |
| 5 | engine.rs | Mid-attempt `?` (transcript write, capture_diff, gate run, commit) aborts the run leaving a dirty/staged tree, no rollback, no report — §14 rollback contract silently skipped | CONFIRMED |
| 6 | workspace.rs | `capture_diff` runs `git diff --cached` without `--no-ext-diff`/color suppression → a user's `diff.external` (difftastic) or `color.diff=always` corrupts the ground-truth diff; every Test task fails provenance, empty-diff check breaks | CONFIRMED |
| 7 | agent/proc.rs | stdin writer thread joined with no grace bound → an orphan holding the stdin read handle hangs the supervisor forever despite the wall-clock timeout | CONFIRMED |
| 8 | workspace.rs | `ensure_run_exclusions` writes `.tactus/` to `rev-parse --git-dir`/info/exclude, which git ignores in a linked worktree → `.tactus/` staged into task commits; `discard_uncommitted` then destroys the run's transcripts | CONFIRMED (empirical) |
| 9 | engine.rs | `AgentError` failure reason renders empty — the Claude adapter signals errors via stdout JSON `is_error`, but `evaluate_outcome` formats the (empty) stderr tail; the most common failure shape yields a blank reason | CONFIRMED |
| 10 | engine.rs | Prompt asserts "Inputs produced by earlier tasks: `<artifact>`" from `artifacts_in`, but no producer is told to emit it and no artifact is materialized → agent hunts a nonexistent file (fires on sample-plan.md's api-contract) | CONFIRMED |
| 11 | config.rs | Pins validated against the catalog but not the adapter registry → a `copilot` pin passes `tactus validate` cleanly, then `tactus run` aborts pre-flight ("no adapter registered") | CONFIRMED |
| 12 | engine.rs | `filename_component(task.id)` is not injective → distinct valid ids (`api/v2` vs `api:v2`, or long ids sharing 64 chars) collide and overwrite each other's transcripts/settings/gate logs | CONFIRMED |
| 13 | plan/markdown.rs | A nested bullet under an Acceptance criterion wipes the parent criterion (`item_text.clear()` on the nested `Start(Item)`) → acceptance data silently lost | CONFIRMED |
| 14 | agent/claude.rs | Rate-limit needles miss the real 2.x CLI phrasings ("5-hour limit reached", "Weekly limit reached", `rate_limit_error`) → genuine limits degrade to `AgentError`, capacity signal (§13) never fires | CONFIRMED |
| 15 | plan/markdown.rs | Checklist/numbered-step titles wrapped across lines concatenate words (`SoftBreak` dropped in `checklist_drafts`) → corrupted slug id in commit messages and `depends=` references | CONFIRMED |

## Also found — excluded by the 15-cap (correctness outranks), tracked here

**Parser (markdown.rs):**
- Multi-line `<!-- tactus: … -->` annotation dropped — `comments_in` scans each `Html` event in isolation and pulldown-cmark emits HTML block content one line per event.
- `##`/`###` headings inside blockquotes/list items create phantom tasks and truncate the enclosing section body (no container-depth tracking in `split_sections`).
- Inline annotation on the heading line itself (`## Fix parser <!-- tactus: … -->`) silently discarded (works for checklist items, not sections).
- Heading-form `### Acceptance` becomes a phantom task; `#### Acceptance` never arms the collector.
- An invisible HTML comment between the `Acceptance:` paragraph and its list disarms collection (`Start(_) => armed = false` matches `Start(HtmlBlock)`).
- `content_hash` over raw bytes → CRLF vs LF makes the same plan hash differently across platforms, contradicting the "stable" doc and risking false resume mismatches.

**Adapter (claude.rs):**
- No `--permission-mode` pinned → deny-by-default rests on an unspecified CLI default (2.x ships an auto-mode classifier).
- An edit agent can commit a `.claude/settings.local.json` that widens permissions for every later task in the run.
- `probe()` advertises hardcoded `Caps` after only `--version` — no real capability probe, so a dropped flag surfaces as per-task errors, not a §19 pre-flight refusal.

**Cross-cutting correctness:** `route::resolve` relabels the first rung `Override → Annotation` when `tier=` equals an override-raised start (misreports decision provenance in the preview); `validate` hardcodes `capacity: not connected` though `pool_names` is parsed; `attempts_per = 0` accepted silently (inconsistent with the timeout-zero rejection); `util::tail` `unwrap_or(start)` can slice off a char boundary for `max < 4` (latent, no current caller); `Workspace::open` doesn't normalize to toplevel so `clean -qfd` is cwd-scoped when run from a subdirectory.

**Altitude (near-term rework warnings):**
- `attempt_task` fuses attempt execution with commit/rollback policy and hardcodes attempt=1 / `PermissionMode::Edit` → step 7's retry-with-resume ladder must dissect it rather than wrap it. Cut a `run_attempt(...) -> AttemptResult` seam now.
- Failure kind is collapsed to `Option<String>` exactly where step 7 (dispatch: RateLimited defers, gate/review retries, Timeout burns an attempt) and step 8 (event payloads) need the structured taxonomy.
- Permission materialization calls concrete `claude::permission_settings` from the engine → step 9's Copilot adapter (argv `--allow-tool`, no settings file) forces a trait change. Move materialization behind `AgentAdapter`.
- `review = { tier = "frontier" }` — DESIGN §17's own example — warns "unknown routing kind `review`"; accept it as a known echoed role before step 6.
- `.git/info/exclude` is the wrong layer; a `.tactus/.gitignore` containing `*` travels with the dir and behaves identically under worktrees (also fixes #8).
- A mid-run environment error discards the whole `RunReport` (committed tasks, spend, session ids); persist per-task incrementally now — the shape step 8's events.jsonl will need.

**Reuse:** `claude::locate` duplicates a weaker PATH walk than `gates::find_program`/`probe_extensions` (already drifted: no PATHEXT); `validate::write_normalized_json` duplicates `engine::write_json`; three graph traversals build three id-indexes (`depends_transitively`, `find_cycle`, `topo_order`); triple temp-git-repo test scaffold (drifted: gates' copy sets no git identity → CI-fragile); test shell builders duplicate `ShellKind::command` (without the raw_arg fix).

**Simplification:** `Draft.ann_found` parallel bool + duplicated annotation intake; unreachable `Config::chain_for` fallback; dead `TaskReport.title`; `Report.task_count` derivable from `plan`; `route` double-relabel; duplicated `ValidateOptions` construction in main; `TaskRun.prompt` split ownership (declared stdin-delivered, engine delivers it separately).

**Efficiency:** `depends_transitively` rebuilds the id-index per (task, artifact) pair; `claude::locate` walks PATH N+1 times per run (cache in a `OnceLock`).

**Conventions:** none — fmt/clippy clean, no `unwrap`/`expect` or `anyhow` outside their allowed places, dependency list intact.

## How they were fixed

| Area | Fix |
|------|-----|
| Adapter invocation | `.cmd` shims build the whole `cmd /C` line themselves and pass it via `raw_arg`, wrapped in the outer quote pair cmd strips — quoted paths and multiple quoted args now survive |
| Rate-limit classification | Detection runs only on failed attempts and covers the shipped phrasings ("5-hour limit reached", "Weekly limit reached", `rate_limit_error`, quota/credits) |
| PATH safety | Resolution moved to `util::find_program`, which skips empty PATH segments (no more CWD-relative binaries) and honors PATHEXT; result cached in a `OnceLock` |
| Sandbox | `--permission-mode dontAsk` and `--setting-sources ''` (verified against the real CLI) so no external settings widen the sandbox; deny list now covers `.claude/**` and `.git/**` |
| Capability probe | `probe()` reads `--help` and refuses at pre-flight when a required flag is gone, instead of asserting fixed `Caps` |
| Abort safety | An error anywhere in an attempt discards uncommitted work before propagating; `report.json` is persisted after every task so an abort keeps the record of committed work and spend |
| Ground-truth diff | `capture_diff` forces `--no-ext-diff --no-color -c color.ui=false` |
| Worktrees / subdirs | `Workspace::open` normalizes to `--show-toplevel`; `.tactus/` is excluded by a self-ignoring `.tactus/.gitignore` (`*`) that works in linked worktrees |
| Supervisor | stdin join is bounded like the read drains, so an orphan holding the read end cannot hang a run |
| Failure reporting | `Outcome.detail` carries the agent's own error text; failures carry a `FailureKind` (the taxonomy steps 7–8 dispatch on) |
| Artifacts | Producers are told where to write each `out=` artifact; consumers get the content inlined, or an explicit note when nothing was produced |
| Run-artifact names | Stems are `<plan-index>-<sanitized-id>`, so id collisions can't overwrite another task's evidence |
| Parser | Container-depth tracking (no phantom tasks from quoted headings), multi-line and heading-inline annotations, acceptance slots that keep parent criteria, heading-form `Acceptance`, comments no longer disarm collection, `SoftBreak` spacing in checklists, CRLF-invariant `content_hash` |
| Config / routing | `attempts_per = 0` rejected; `review` accepted as a routing role (DESIGN §17's own example); pins validated against the adapter registry in `analyze`, so validate and run agree; annotations no longer take credit for an override-held floor |
| Preview | Capacity line names connected pools instead of always claiming "not connected" |
| Reuse / simplification | Shared `util` (tail, filename sanitizing, PATH resolution, JSON/text writes); one `RunCx`/`AttemptCx` pair replacing the 9-arg attempt call; `Draft.ann` is an `Option`; derivable `task_count` dropped; single `ValidateOptions` construction point; `index_by_id` built once per pass |
