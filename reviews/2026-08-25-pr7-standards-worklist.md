# PR7 — `CODING_STANDARDS.md` conformance work-list

**Not S5 findings.** This file collects conformance observations raised during PR7's
review rounds so the G2 pass can ingest them. It is empty until reviewers file into it.

## The rider reviewers are given, verbatim

> `CODING_STANDARDS.md` conformance findings do not block this merge. File each to the
> sweep work-list with the section it cites (or "no citable section"); only contract and
> correctness findings are S5 findings. The slice predates the standard; conformance is
> the G2 pass's remit.

## Why this file exists rather than the ledger

The PR's `Review finding ledger` is for findings that gate the merge — contract and
correctness — and its canonical row shape requires a severity, a provenance, a category
and a disposition. A conformance observation has none of those in the sense the ledger
means them: it is not a defect against the packet, and its disposition is "the pass will
decide", not "fixed" or "rejected".

## Salvage

Entries here are **salvaged by-hash against the merged head**, per the pass proposal's
W10.4. Each row therefore records the file and the exact content hash of the region it
cites, not a line number: PR7's own history has three occurrences of a line-anchored
reference going stale under `cargo fmt` alone (`reviews/FINDINGS.md` §4), and a work-list
that cannot be salvaged is a work-list that gets re-derived.

## Entries

### W10.2 — the layer sweep's work-list

Filed by the standards review of `3e5212d98b20e2cf72d2fc3982746c7e87de4034`. Every row is salvaged by the sha256 of the
cited region, per W10.4: re-locate the region by hash, never by line number.

| Observation | File | Region sha256 | Standards section cited | Raised by / round |
|---|---|---|---|---|
| The module comment claims seven remaining names immediately above only three declarations. — The cited declaration block contains `preflight`, `recover`, and `run`, not seven names. The surrounding explanation repeatedly relies on seven as the derived public surface, so it no longer describes the code beneath it. | `src/engine/topology.rs` | `85e29280559dc1e8b9d92e75c6ef016efc9c253962fc6e0fec75da49520c0443` | §4 formatting, naming, and readability | standards review of 3e5212d / L5-engine-topology-tests |
| The runner-failure path explicitly discards a possible invocation-ledger cancellation error. — Cancellation is the protocol settlement for the registration made immediately before this path. Dropping its result makes a failed settlement unobservable while returning an unrelated runner error. | `src/engine/topology/attempt.rs` | `5bea6267552c02899e43abcd5d369280787f370f3ea416eb83fe52d6ebdb8a23` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| A reviewer snapshot survives an ordinary adapter-lookup error because cleanup occurs only on the later success path. — The snapshot is created before a fallible adapter lookup whose `?` returns it nowhere. The corresponding `remove_snapshot` is later in the loop, so this error bypasses cleanup of the locally owned worktree resource. | `src/engine/topology/attempt.rs` | `847506a3c233530bf39ca0c50b4beb52d93da998b7a39d8356d46cde94a8cf85` | §6 ownership, mutation, and resources | standards review of 3e5212d / L4a-engine-topology-prod-a |
| `AttemptContext` documentation still describes eight dependencies although the struct now has ten fields. — The comment presents its count as the reason for the bundle’s shape. The additional review and input-policy dependencies make both “eight parameters” and “six of the eight” stale. | `src/engine/topology/attempt.rs` | `ff7af4839e2e6eb42da57b5959cdf7c6d94db8ea2c1f8d5b0553bfc1e2fdbe67` | §4 formatting, naming, and readability | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The default sampler’s pass condition depends on a scheduler-sensitive polling and sleep race that must land at least once. — Kill timing depends on wall-clock polling around an uncontrolled Git child. The test later asserts that at least one kill landed, so identical inputs can pass or fail according to host speed and load. | `src/engine/topology/attempt/tests.rs` | `f357ddf6d4d78fa02f9759e95591a5783589cbfef8a6d34502e6dd526fdaff8b` | §12 tests | standards review of 3e5212d / L5-engine-topology-tests |
| The default sampler overwrites a checkout-relative evidence file with machine-varying data and immediately reads that shared file back. — Earlier in the test, `path` is the manifest directory joined to `effects/attempt-residue-histogram.json`, and the emitted payload identifies itself as machine-varying. Running the test changes the checkout, while concurrent invocations contend on the same file. | `src/engine/topology/attempt/tests.rs` | `f7e4a7de9305429cb1c49bc6becc57728e262277b2e574ddf91763d399980cd0` | §12 tests | standards review of 3e5212d / L5-engine-topology-tests |
| The test fixture’s RAII cleanup silently discards failure to remove its scratch tree. — `Fixture::drop` reaches this helper, which ignores the removal result and exposes no cleanup outcome. A failed teardown can consequently leave repository and private-root residue without any test failure or diagnostic. | `src/engine/topology/candidate.rs` | `38ab7df7ec377e38ad4c9e32806f3250d4efeff1124b77ce482c1b847e2336b8` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The test documentation still says recovery has no recorded tree even though the production recovery and the following test now carry and verify `tree_sha`. — `PromotingCandidate` carries the tree retained by the durable prepared record, and the immediately following test exercises that comparison. This paragraph describes the superseded implementation gap as current behavior. | `src/engine/topology/candidate.rs` | `b5167460d86d806bdb1c6afbbeb573a9ac42b40cb54252a497a1676e36989861` | §4 formatting, naming, and readability | standards review of 3e5212d / L4a-engine-topology-prod-a |
| `expected_refs` allowlists a prepared pin for every historical generation, including closed generations whose pin was already required to be pruned. — A pin introduced for an older closed generation therefore appears authorized to the unexpected-ref check. This file recovers only a task’s last generation, so such contradictory Git evidence is neither refused nor subsequently examined. | `src/engine/topology/candidate.rs` | `f5512f4b6fb3646ae43efc43d93ebf0973e1414b7a021cfd5c3596f550140e08` | §14 security and trust boundaries | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The durable `run_started` record stores the private directory through a lossy UTF-8 conversion. — `private_dir` is a persisted run locator consumed during replay and recovery, not display-only text. A non-UTF-8 path can therefore be recorded as a different path identity. | `src/engine/topology/create.rs` | `20e8ec453fe4d87b6bf64fde5688647ae21755095a96ddd795812c71af4079c3` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4a-engine-topology-prod-a |
| Public-husk removal failures are discarded while the returned dispositions assert that removal completed. — Both branches suppress the removal result and return `BothHalvesRemoved` or `PublicHalfRemoved`. The failure is therefore unobservable through the returned value, which can affirm removal that did not happen. | `src/engine/topology/create.rs` | `b4c859f6fdb8efbdb9055fc365a95523e551537d43fbed1b99442cceb658747b` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| `Refused::into_error` stringifies and discards the structured `UpstrokeError` that stopped creation. — The boxed source is interpolated into a new generic `Refused` message rather than retained in the error chain. Callers consequently lose its structured category and source while the textual fragments are joined with sentence punctuation. | `src/engine/topology/create.rs` | `cd4c0a784d91d1992bc283c441f920b6f5dfeeea773ca0e39e012c95f4fc8651` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| `canonical_string` lossily converts public-directory identity used by owner and commit records. — Both record types use this helper for an ownership-proof field. Even after successful canonicalization, `to_string_lossy` can replace non-UTF-8 bytes and make the persisted identity differ from the filesystem path. | `src/engine/topology/create.rs` | `f1ceb9f2c0ce5fba12d17b7c95d05ea1a1d1a303985518206b4909b80539dce8` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The creation marker records its private-half locator through a lossy UTF-8 conversion. — `CreatingMarker.private_dir` is later used to locate and prove ownership of the private half, so it is identity rather than diagnostic text. Lossy conversion can collapse distinct OS-native paths, and this field is not the recorded `CommandSpec.program` exception. | `src/engine/topology/create.rs` | `f7dc27ce839a99c324cf7f4a1ee964f5ac743277342701c429d02195cef0d46d` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4a-engine-topology-prod-a |
| `canonical_string` deliberately substitutes an unverified lexical path when canonicalization evidence is unavailable. — The helper feeds records later compared by the private-half ownership proof. When canonicalization fails, two equally unverified fallback strings can compare equal and participate in authorizing deletion. | `src/engine/topology/create.rs` | `fd1cf5aa14647b536ff39cc5bf86568bb9435d93702933b7baf7b3bd08489209` | §14 security and trust boundaries | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The P8 source scan does not assert that its blanker removed any comments or literals. — Finding `run_recovery_order` only proves that some production text remains. The full scan contains no blanked-region count and does not declare a domain intentionally containing no removable regions. | `src/engine/topology/create/tests.rs` | `0b2d7cdd2520dd910f74354d63784222b432a53ddc76de49d0cfbeb44422c7b2` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The committed-line helper erases both log-read and JSON-parse errors into `None`. — Permission or transient read failures become indistinguishable from a missing line, and malformed persisted data becomes the same absence. The helper is not identified as best-effort and provides no error context to its callers. | `src/engine/topology/create/tests.rs` | `0d92d6da8020fc9b279dd8316debdfe3ef6ddea8bc54ab8ee96e88ebee4529bf` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The intent census reports an empty or incomplete set when directory enumeration fails. — A directory error becomes an empty vector and `flatten()` omits failed entries. The same helper feeds an `is_empty()` cleanup oracle, so those errors can be accepted as successful reclamation. | `src/engine/topology/create/tests.rs` | `5edfcef18285a60f234c3536ef7f6a64dcde77d389f85be389f5a484cb116140` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The directory-name oracle converts directory failures into an empty directory and silently drops failed entries. — Any `read_dir` error returns `Vec::new()`, while `flatten()` discards individual entry errors. The P3a test has an expected-empty case, so an unreadable directory can satisfy that oracle. | `src/engine/topology/create/tests.rs` | `bf17903f97dee346e3cc16ac91e96498d6a7679b38adad07d9f05350397d5579` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The create-test fixture allocates a unique temporary root but has no RAII cleanup owner. — The full module defines no `Drop` implementation or cleanup guard for `Fixture`. Its own comment acknowledges that the fixture cannot remove its root, so every invocation leaves the tree behind, especially after failures. | `src/engine/topology/create/tests.rs` | `e5a315d3bced9d846348697ed4f7ed25399c8b22f2499f2262ff34276690e0fb` | §12 tests | standards review of 3e5212d / L5-engine-topology-tests |
| The fragment `committed.` is attached as rustdoc to the publication-order test instead of completing the preceding test’s documentation. — The preceding documentation ends with “the log is”, while this fragment appears after that test’s closing brace. Consequently the next test receives a sentence fragment unrelated to its item. | `src/engine/topology/create/tests.rs` | `f9121a9404c6c0937f8b9110af8735ef1145b27ac015325a60c6be911863f51f` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| `task_dispatched` persists the worktree’s durable identity through a lossy UTF-8 conversion. — The comment identifies this value as one a later process compares and re-derives, making it an identity field. Distinct OS-native worktree paths can serialize to the same replacement-character string. | `src/engine/topology/dispatch.rs` | `cccb7b32f024c414919181d4a43930202268bbbc2fca01b574bf5386847e9999` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The append-protocol witness exposes every field publicly and is therefore forgeable by sibling crate modules. — The type’s documentation treats its existence as evidence that append-error obligations already ran, with only cancellation outstanding. Public struct-literal construction admits values for which no append or protocol occurred, after which `cancelling` can mint an `AppendError`. | `src/engine/topology/emit.rs` | `1839d0f27629fc2b2ac12e250cf7a9c49e543364272eb0ce894b432c5a86f415` | §5 types, modules, and APIs | standards review of 3e5212d / L4a-engine-topology-prod-a |
| Discharging an append failure stringifies its structured report and underlying `UpstrokeError` cause into a generic refusal. — `AppendError` retains the original cause structurally until this conversion. Calling `to_string()` erases that source and its category from the propagated error chain. | `src/engine/topology/emit.rs` | `fcc6f0d745774e1b4240a3718ce119a1eba20e22c13c8d747e5e4a668bca3615` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The emit-test scratch constructor creates temporary run trees without a module-owned RAII cleanup path. — The returned value is a production `RunPaths`, and the full test module defines neither a cleanup guard nor removal for these roots. The PID and counter distinguish live in-process fixtures but do not reclaim their directories. | `src/engine/topology/emit/tests.rs` | `3e62f9664a79b3316c417c8235405c3ff31a85403ef9a685f426563dc7aa9e2b` | §12 tests | standards review of 3e5212d / L5-engine-topology-tests |
| Slot classification is independently implemented here and in `ExecutionRole::is_slotted`. — The comment explicitly identifies both functions as readings of the same agent-pool rule, while this function separately matches every role variant. A test pinning this side does not turn the two production authorities into one. | `src/engine/topology/identity.rs` | `2ef5480a695a91feb1ff11e23a34317aa2601cd48d37f3653fb2d766ee64f085` | §3 Rust-native design principles | standards review of 3e5212d / L4a-engine-topology-prod-a |
| `SlotPair` represents its agent identity as a raw `String` despite the available `AgentId` domain type. — The field is the key for per-agent capacity accounting rather than presentation text. Its raw representation removes the type distinction between agent identities and arbitrary strings. | `src/engine/topology/identity.rs` | `4785edbfda4c9aa8bc928790195050ab9a768f85db2bf6e6612e9e24790bb503` | §5 types, modules, and APIs | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The missing-agent path discards the invocation-ledger cancellation result before returning its refusal. — Registration has already succeeded, so cancellation is the required settlement of that registration. Ignoring its result can return the missing-agent error while leaving the ledger failure unobservable. | `src/engine/topology/preflight.rs` | `0182e455b0d3b274f71ce7c5b5aa1bbb1ea9123d5e57c9e9b7281041edb4e3b9` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The slot-acquisition failure path discards the invocation-ledger cancellation result. — Cancellation is the only settlement recorded for the registration when slot acquisition refuses. Returning only the acquisition error suppresses any failure to perform that protocol transition. | `src/engine/topology/preflight.rs` | `59e87c5dc3ac4808cf0f823dfe715d49fa447c46d0bf48f29c025b597c5b5240` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The pre-flight refusal’s error text has a trailing period. — The final literal stored in `UpstrokeError::Refused` ends with `resumable.`. This message is the error’s displayed operational context, not separate prose. | `src/engine/topology/preflight.rs` | `6bd7fb8d4ad0f742f238e410770910204ed31a429a17101db50719443c0e715d` | §7 errors and panics | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The module documentation says the Codex adapter runs four probe processes although the same file later enumerates ten. — Later documentation lists version, two help probes, six strict-config probes, and the model catalog. The stated count and the conclusion that only three processes would be unaccounted are therefore stale. | `src/engine/topology/preflight.rs` | `bde2f1d42c99c7b924eedd381a59aa9ea704eefe96a6eb1768b164727d332f26` | §4 formatting, naming, and readability | standards review of 3e5212d / L4a-engine-topology-prod-a |
| The pre-lock test helper creates unmanaged temporary directories without RAII cleanup. — The helper returns only a `PathBuf` after creating the directory; no guard owns it and no removal occurs in this file. PID/thread-derived naming does not supply cleanup on success, early return, or panic. | `src/engine/topology/prelock.rs` | `24d9fc75c8bf979a3c5bc556419e750b0bd753e65f85a9e5b3281d24c6569bb2` | §12 tests | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The explicit private-root comparison falls back to lexical equality on every canonicalization error. — `normalize` is used for the comparison between the explicit root and the recorded authorized root. Permission, transient I/O, and other canonicalization failures all become lexical paths, so matching spellings can pass while canonical identity is unavailable. | `src/engine/topology/recover.rs` | `31b6eba9705daa9e949c4ed66696e6e9c09e763fda1f75230333ab61072eafb4` | §14 security and trust boundaries | standards review of 3e5212d / L4b-engine-topology-prod-b |
| Owner-record public-directory authentication falls back to lexical spelling when canonical evidence is unavailable. — This value is compared with `owner.public_dir` while authenticating the private half. Any canonicalization failure is discarded, allowing the ownership comparison to continue using a lexical path. | `src/engine/topology/recover.rs` | `388c5fbd6fab41a9c3fc9abb90aa0796b9953e2a418fdeb0dc61fd27fa8014bd` | §14 security and trust boundaries | standards review of 3e5212d / L4b-engine-topology-prod-b |
| Recovery allocates each persisted ownership or commit record without an input-size bound. — `owner.json` and `committed.json` are persisted inputs and therefore cross a trust boundary. `read_to_string` allocates through end-of-file before deserialization, with no bound at this read site. | `src/engine/topology/recover.rs` | `dccd74f5f677f8bd746cfd8d1959b468a33373d5ad8633dae36a716080e8cb74` | §14 security and trust boundaries | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The recorded private-root locator is accepted without absolute-path or symlink/reparse-point containment validation. — This validation rejects `ParentDir` and checks the final two components, but it does not require an absolute path or inspect symlink/reparse-point traversal. A relative or redirected persisted locator can therefore pass the structural check used to derive the authorized root. | `src/engine/topology/recover.rs` | `f2aa6763a72ff901dda7a55bfb71c583343276162706c926334a19363dd73284` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The packet caller census claims an eleven-entry domain, but `CLAUSES` contains ten entries and no assertion pins that boundary. — The census iterates whatever entries happen to be in `CLAUSES`, so an omitted clause is not observed. The stale eleven-versus-ten count is concrete evidence that the claimed domain has already drifted without failing the test. | `src/engine/topology/recover/tests.rs` | `1ea7b3137886f378c905252e1582fc4ecb98b903ffb1a8c1bc724d4bb5d2224f` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The recovery fixture’s RAII cleanup discards every removal error without observability. — The comment describes reclamation as necessary to avoid inode exhaustion, but the result is ignored. The module records no cleanup failure and has no test for this best-effort failure path. | `src/engine/topology/recover/tests.rs` | `4374698173c45e9fc1d2f3fca1e51e2f70a7dd22dcb5d76c7ea854b9938c2149` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The `attempt_finished_failing` documentation says `attempt_finished` records `failure: None`, although the current helper records `Some(FailureRecord)`. — The immediately preceding helper unconditionally sets its record’s failure field to `Some`. This comment consequently explains the derived helper using a state the base helper no longer creates. | `src/engine/topology/recover/tests.rs` | `636a37c1139d2ba14f656961e2c6f32880ff7534404f5a3f30faf6bed6599456` | §4 formatting, naming, and readability | standards review of 3e5212d / L5-engine-topology-tests |
| The retained-tree oracle substitutes zero bytes when reading a file fails. — A partial tree snapshot can contain an invented empty file instead of surfacing the read error. Since snapshots are compared as byte-identity evidence, matching failures can produce a green comparison. | `src/engine/topology/recover/tests.rs` | `65c341cf6561fcd04762f1f2d2bfca6e2eed36cca1e6efafaeb3ed5c49e04f22` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The spend-parity documentation says successful attempts append both `attempt_finished` and `candidate_prepared`, contradicting this file’s current sole-successful-settlement model. — Earlier current documentation states that `candidate_prepared` is the sole successful settlement and that `attempt_finished` represents failure. This historical double-count explanation therefore describes an event sequence the current model excludes. | `src/engine/topology/recover/tests.rs` | `68b7a3228d4ceaf90f04b164810d5040c5c1cc3ca0e96b3408c382dcefdea4bf` | §4 formatting, naming, and readability | standards review of 3e5212d / L5-engine-topology-tests |
| The reference-effect recorder records an empty log snapshot when reading the actual log fails. — Tests use these snapshots to establish what was durable when the ref operation began. A read failure is therefore admitted as false ordering evidence rather than observed as a fixture failure. | `src/engine/topology/recover/tests.rs` | `a066091cd5bb3276be88a54dcf3e843b81355a2b4f04647d6dff546f8ad3fde3` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The barrier census uses a length ratio that cannot establish the required nonzero blanked-region count. — For a conforming blanker, `region` and `whole` have equal lengths, making this threshold automatically true. It therefore cannot distinguish a functioning blanker from a scan that removed no comments or literals. | `src/engine/topology/recover/tests.rs` | `c3d613d3dd6f1aa0f33c77a22b8a0544938a62a00555239c5b0770213e14a9ce` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The recovery fixture silently substitutes an empty log for every bounded-read failure. — Numerous recovery assertions use these bytes as their before/after or replay evidence. An unreadable log can therefore be treated as a real empty log, with no defined observability or failure-path test. | `src/engine/topology/recover/tests.rs` | `cf4d6628cdc470f3c68ee891e1b8203151c4699e5e208e0c36b3c153c0c13807` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The test name claims that halted recovery finalizes, while its documentation states that the test proves refusal and finalization’s absence. — The documented scope explicitly excludes `RunDir.WriteReport` and asserts no report. Thus a failure named `resume_finalizes_halted_then_refuses` does not read as the claim actually exercised. | `src/engine/topology/recover/tests.rs` | `eb0e8ed9b3480b8b2d2bc95747b4ddc60a17a6659a044335d708e980e0ee71ec` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The real-file call-matcher scan never asserts that any blanked region was present. — The guard permits `whole == region`, because it only requires each count to exceed an independent floor. Thus a blanker that removes nothing can satisfy the assertion despite the comment’s claim that three occurrences were removed. | `src/engine/topology/recover/tests.rs` | `fd3859b3671bcb471fbc2ddf13c29a146a766c274156995b89bb99fd8591dfb9` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| Successful-attempt spend and feedback projections are mutated before `candidate_prepared` is appended. — The projections are advanced before `append_candidate_prepared` invokes the journal’s event append. A pre-entry refusal or failed append therefore leaves process-local projections ahead of the authoritative event prefix. | `src/engine/topology/run.rs` | `4307e4037911763ea5dfed24ff3fc1d7b54a84069a56403d9db865f6ab8bcea8` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4b-engine-topology-prod-b |
| `RunAs::feedback` is documented as empty for a first dispatch even though a new generation can carry accumulated task feedback. — The dispatch arm supplies `self.brief.lines(key)` to the first attempt of a new generation after sessionless retries or escalation. Thus “first dispatch” does not imply that there is nothing to tell the worker. | `src/engine/topology/run.rs` | `538af9fa48f70bbd129ae52eb06d972a519548813c320650bf80bd953315a60c` | §4 formatting, naming, and readability | standards review of 3e5212d / L4b-engine-topology-prod-b |
| Failed-attempt spend and feedback projections are mutated before the settlement event is appended. — `Spend` and `Brief` are explicitly replay-derived projections of durable settlements. If `emit` refuses before entry or its append fails, both projections have already incorporated a settlement not established by the event log. | `src/engine/topology/run.rs` | `7402bc80fe66bf0c28a6a333ec3a3162cf8352d59da32e97635e2831b79efdc3` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The dispatch failure path silently discards a reservation-cancellation error. — Cancellation is not identified as best-effort and its failure has no observable channel here. The caller receives only the original dispatch error and cannot distinguish a failed ledger cleanup. | `src/engine/topology/run.rs` | `881fe02b3e7273bc465aec97e39cc0158700acf609366fc1df860d1d4cf7229d` | §7 errors and panics | standards review of 3e5212d / L4b-engine-topology-prod-b |
| `Progress::Settled` is documented as ending in `attempt_finished`, although accepted attempts settle through `candidate_prepared`. — The accepted branch calls the candidate promotion sequence and returns this same variant without emitting `attempt_finished`. The comment therefore misstates the durable event contract for one of the variant’s normal outcomes. | `src/engine/topology/run.rs` | `bdf10f106188ee938ec387ddbb5e40da0a624f07550118806106ffd1b5a771d3` | §4 formatting, naming, and readability | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The attempt-started census examines only the first constructor in each hard-coded file. — `.find("AttemptStarted4 {")` selects one occurrence, and the following slice stops at that occurrence’s first closing delimiter. The later `checked == SITES.len()` assertion proves only one match per listed file, so a second constructor lies outside the scanned domain. | `src/engine/topology/run/tests.rs` | `2263b279b639578eb19aa3183682c6e053b8684342277ebc56808eec0ca2f50f` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The frozen-pool census also treats a length-preserving output ratio as evidence that blanking occurred. — The `* 2` threshold is automatically satisfied when the blanker obeys its required length contract. The later synthetic call-needle controls exercise identifier matching, not the presence of blanked regions in this scan. | `src/engine/topology/run/tests.rs` | `50e062632e6bc4a2c9cd1d14518a35b33b5886588a6803605bf5e1b78264cad5` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The selector census repeats the same length-preserving blanker check instead of asserting a nonzero blanked-region count. — `code.len()` is expected to equal `source.len()`, so this check passes regardless of whether anything was blanked. Counting the real selector calls afterward does not exercise the blanking premise. | `src/engine/topology/run/tests.rs` | `97890c0862224cca323e83b91a9f114a10afacb8fbef823282c76afa6d9a7d4b` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The append census’s length guard cannot assert a nonzero blanked-region count. — A conforming `production_code` result has the same length as its source, so multiplying that length makes the guard tautological. The later append-site floor does not establish that any comment or literal was blanked. | `src/engine/topology/run/tests.rs` | `f7f60835ecb2f42bfee675b2c1726dac6176cbe8a1b5b11e36a989d59cc95e8c` | §12 instruments and censuses | standards review of 3e5212d / L5-engine-topology-tests |
| The scaffold converts execution-root and private-directory identities with lossy UTF-8 conversion. — These values populate `RunStarted4` path fields used to identify the run’s execution and private roots, rather than diagnostic text. On a non-UTF-8 path, replacement characters can make the recorded identity differ from the actual path. | `src/engine/topology/scaffold.rs` | `3c17f18d47c3a8a6a84331ad43b87221786c5a505c3c75b9b1d857b02ec55eaa` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The kill-test handoff helper creates an unmanaged temporary directory without RAII cleanup. — The helper returns a plain `PathBuf`, and the handoff directory has no cleanup owner. The PID/ordinal naming prevents ordinary intra-process collisions but does not remove residue after success, failure, or child abort. | `src/engine/topology/scaffold.rs` | `b76eba78f74dba6974d0e59ca1bbb9cb368148f34eec54b7e735558220046b5f` | §12 tests | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The spawn-order test oracle collapses log read and parse failures into an empty durable history. — An unreadable log and a malformed log both produce the same `Vec::new()` as a genuinely empty log. The helper is not documented as best-effort, so callers cannot distinguish fixture failure from the ordering fact they are measuring. | `src/engine/topology/scaffold.rs` | `c6e203409dd7035ebd3df9830c5aa4949fdba8ab0d3c894ec2cf75a9b95a8b0a` | §7 errors and panics | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The `arm_label` structural census scans raw source text and string substrings without blanking comments or literals. — `body` is a direct slice of the source file, and `match_indices` treats any matching text in comments or literals as structure. Braces or label-like text in those regions can change the selected domain or counted labels without changing the Rust match. | `src/engine/topology/select.rs` | `ad4aa3f1e23475c8fa789580b6d72998b3343a25c93850a9ab05532c04fd3c02` | §12 instruments and censuses | standards review of 3e5212d / L4b-engine-topology-prod-b |
| Reported spend is accumulated and budgeted without rejecting non-finite floating-point values. — The unguarded additions can accept or produce `NaN` and infinity in both totals. These totals feed ceiling comparisons, where `NaN >= limit` is false and can suppress a budget breach. | `src/engine/topology/select.rs` | `ec7a9121128ec9de01b6f2bce553f0e73a60c3ddec95ef20acfd4ad62dbf37d3` | §5 types, modules, and APIs | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The settlement kill tests create unmanaged temporary directories without RAII cleanup. — The helper returns a plain `PathBuf` after directory creation and establishes no parent-owned guard. Its PID/ordinal naming does not clean residue after the parent test completes or fails. | `src/engine/topology/settle.rs` | `87dba4364f53b44257d01eb1bc83a3884f6012d8c822e0422a493da3b27b44b9` | §12 tests | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The defer-round shadow state advances before the caller appends `defer_wait_elapsed`. — `round` determines the next backoff duration and is copied into the event returned to `TopologyRun::step`. If that later append refuses or fails, the in-memory schedule has advanced without a corresponding durable transition. | `src/engine/topology/settle.rs` | `89b0acfb686e53332721c6eb1d5a1adcfc265ca983eb1c8c7bbf88dc40ea7c05` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L4b-engine-topology-prod-b |
| Stale-marker detection treats every metadata error as marker absence. — Both `symlink_metadata` results are reduced with `is_ok()`, so permission and transient I/O failures become `false`. When neither probe succeeds, the census can classify the committed directory as having no stale marker rather than preserving the error category. | `src/engine/topology/startup.rs` | `b1471a4a47b9380175f45f4c57c3d6a7bcd45fe414c30b3d7e817942d9ed3538` | §7 errors and panics | standards review of 3e5212d / L4b-engine-topology-prod-b |
| The startup fixture’s RAII cleanup discards its removal error without observability. — Cleanup is the operation intended to prevent leaked fixture trees, yet its result is ignored. The module defines neither a record of failure nor a test of that best-effort path. | `src/engine/topology/startup/tests.rs` | `33702a8da7f3727e898bb6a1242777867f12a1bb8a4d6983ca0185a1c4e38dba` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The startup existence oracle maps every metadata error to absence. — `is_ok()` returns false for permission and transient metadata failures as well as `NotFound`. Tests asserting nonexistence can therefore pass without establishing actual absence. | `src/engine/topology/startup/tests.rs` | `3cbc23a544f58b6e2aee19cb6c38ea17c1c272f20880b8ccc077c4600c26f212` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| The startup tree census skips unreadable directories, failed entries, and unreadable files instead of preserving those errors. — `continue`, `flatten()`, and the conditional file read all turn failures into a partial snapshot. Two partial snapshots can compare equal and satisfy the byte-retention oracle. | `src/engine/topology/startup/tests.rs` | `42b0119a09fe125492b5b7bb6aeaf43caa24af93838199b2bad81b8f7aec6cbc` | §7 errors and panics | standards review of 3e5212d / L5-engine-topology-tests |
| Startup fixtures use a caller-name-only temporary path, allowing concurrent identical test processes to delete and reuse the same directory. — The path has no process identifier, nonce, or unique-directory owner. The unconditional pre-removal means one invocation can erase another invocation’s live fixture rather than merely encountering stale state. | `src/engine/topology/startup/tests.rs` | `c664981ca1a97d9e87e5b4fb11eafb0245bf48a037a246a3db59a8a32ab2a5ae` | §12 tests | standards review of 3e5212d / L5-engine-topology-tests |
| The `refused_labels` rustdoc promises only refusals, but its implementation also labels state-ceiling truncations as refusals. — The helper implements `false` as “not accepted,” which includes both `Refused` and `Truncated`. Because truncation is a distinct search-ceiling outcome, the method returns labels that the fold never refused and its public rustdoc states a false contract. | `src/topology/census.rs` | `c687c1f0eba5b58e963cbe3f686094a67639ace95341ce13070f1ad6d10a343d` | §13 documentation and observability | standards review of 3e5212d / L3-topology-rest |
| Report.Write and RunDir.WriteReport are two funnel implementations claiming the same report.json write. — The cited comment states that both funnel groups model the same durable report object and that both are implemented solely because both names exist. This is exactly the duplicate-authority case the standard identifies as a finding. | `src/topology/effects.rs` | `044669b6b8d2488329576be3c83ad7c0dbc82e9ea620c383c36fda15b5df7974` | §3 Rust-native design principles | standards review of 3e5212d / L2-effects-wire |
| RegistryError::ResidueClaimsExecution renders Display text with a trailing period. — `thiserror` uses this literal as the variant's Display implementation, so the emitted text ends in a period. Appendix A leaves this context-quality requirement to review. | `src/topology/effects.rs` | `6a0590b2226afcb22685f6dd1e298e013e2346eb44d2ca31a98ba38bab1d7307` | §7 errors and panics | standards review of 3e5212d / L2-effects-wire |
| Saturating sample-count arithmetic can accept a contradictory fault-evidence histogram as fully accounted. — The component sum is capped at u32::MAX before comparison with `n`. With `n == u32::MAX`, an already-maximal histogram and `unclassified == 1` still produce `counted == n`, accepting evidence that claims more samples than its declared total; the named small-count test does not cover this overflow boundary. | `src/topology/effects.rs` | `9273850d324823aab61b2815cf59c1e8b8ccf8467b10513f0d2498d2b25e6a90` | §14 security and trust boundaries | standards review of 3e5212d / L2-effects-wire |
| The durable event timestamp is represented as an unrestricted String. — Deserialization and the public field admit arbitrary text as `ts`, leaving malformed timestamps and unrelated strings in the same type. This wire type supplies no timestamp-specific validation boundary. | `src/topology/events.rs` | `23cee994aa4780e2b9f45775b7bec53b4b9f6f23959b4889c724fb1887ec60dc` | §5 types, modules, and APIs | standards review of 3e5212d / L2-effects-wire |
| CommitSha claims a full commit identifier while exposing unchecked construction from every string. — The public representation and `From<&str>` accept empty, abbreviated, or non-hex strings without validation. Consequently, a value named `CommitSha` does not preserve its documented full-SHA invariant. | `src/topology/events.rs` | `3ecce88d442c23b1bf2217f4e6f24118734547d3fd6d04e553beecbf0650df66` | §5 types, modules, and APIs | standards review of 3e5212d / L2-effects-wire |
| The wire-vocabulary constructor discovers wall-clock time through a global utility instead of receiving a value or capability. — This module describes itself as the topology-log shape, but this constructor reaches ambient wall-clock state and offers no timestamp or clock capability to callers. The mechanical denylist can pin direct clock paths but cannot establish the semantic placement of this indirect dependency. | `src/topology/events.rs` | `839f537e7b5dae942dc32e54f638607fe6a2dde5f66d51ce25829192e535eedd` | §3 Rust-native design principles | standards review of 3e5212d / L2-effects-wire |
| RunStarted4 records operational filesystem path identities as String and Option<String>. — These values are filesystem identities used during containment and recovery, rather than diagnostic display strings, yet they assume a UTF-8 representation. The cross-platform rationale in the comment is not a recorded exception; the standard's known path conflict covers only `CommandSpec.program`. | `src/topology/events.rs` | `a85be4fe15cf6b9194f76465f89c1ebe2028a2eb4897ccfd89a78e7c1c51ab0c` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L2-effects-wire |
| GitRef claims a full Git ref identity while exposing unchecked construction from every string. — The infallible conversion and public tuple field accept arbitrary strings, including malformed names and values outside `refs/...`. The wrapper prevents a Rust type mix-up with a SHA but does not preserve the ref-name invariant it documents. | `src/topology/events.rs` | `bcb4b7203448ab3ef66edd81239b30bc159795bd679c786d6d2d82b86748d610` | §5 types, modules, and APIs | standards review of 3e5212d / L2-effects-wire |
| AttemptNumber admits zero even though its documented domain begins at one. — The derived `Default` produces `AttemptNumber(0)`, and the public tuple field also permits zero directly. The type therefore carries a documented invalid attempt number as an ordinary value. | `src/topology/events.rs` | `d0bfde1147f93ecd78251b8fd702654adb8fdd80865d2906cd314bce16e0c099` | §3 Rust-native design principles | standards review of 3e5212d / L2-effects-wire |
| The required-field deletion sweep omits every optional field whose canonical wire value is null. — Filtering null values before selecting keys means the test never exercises absence of required optional keys such as `halted_at`, `prepared_ref`, and `verification`. Those fields' `strict::required` attributes therefore lie outside the claimed missing-input test domain even though the test suite executes successfully. | `src/topology/events.rs` | `d6a2d15aa1d2432f801b8ebc03145f99f73ef13d20580da456f5b6869caf406f` | §12 tests | standards review of 3e5212d / L2-effects-wire |
| FoldError::RewrittenLog renders a Display message with a trailing period. — The final literal segment is `wrong.`, with no interpolation following it, so every rendered `RewrittenLog` message ends in a period. The green panic-policy gates do not cover error-message punctuation. | `src/topology/fold.rs` | `260e12fc1a63d79b39089480fc1ec10954e4f264b677eedbc1cc5cdcfb4b1062` | §7 errors and panics | standards review of 3e5212d / L1-fold |
| FoldError::MalformedLadder renders a Display message with a trailing period. — The final literal segment is `climbed.`, with no interpolation following it, so every rendered `MalformedLadder` message ends in a period. This is review-only error-text quality rather than the mechanically enforced panic policy. | `src/topology/fold.rs` | `28aecb030189cbb5a49231af0fd96c6acf1065593c2052f296a43d4ee99d78e1` | §7 errors and panics | standards review of 3e5212d / L1-fold |
| The frozen-binding hostility table omits the independently meaningful pinned field from its field-by-field cases. — `BreakBinding` mutates agent, model, tier, and effort, while `RungBinding::pinned` is absent from this table and is never perturbed elsewhere in the file. A validator that ignored `pinned` would therefore still pass the claimed field-by-field test. | `src/topology/fold.rs` | `3a70052cdd0b853cc70f7aa07d1757e4a7203435bcbeffbd465654ce0c1fca08` | §12 tests | standards review of 3e5212d / L1-fold |
| FoldError::BindingMismatch renders a Display message with a trailing period. — The final literal segment is `identity.`, with no interpolation following it, so every rendered `BindingMismatch` message ends in a period. This falls within the row’s review-only context-quality half. | `src/topology/fold.rs` | `42356c852dc9d45ce856a70b5869df8349f3ccf69d93c495fbc2db52898f19f7` | §7 errors and panics | standards review of 3e5212d / L1-fold |
| FoldError::RunnerMoved renders a Display message with a trailing period. — The final literal segment is `life.`, with no interpolation following it, so every rendered `RunnerMoved` message ends in a period. The named panic lints do not enforce this prose constraint. | `src/topology/fold.rs` | `540b7a0a14afd0c4126708153f2469449f6e375b696d966e7a75ddf71efe0ea3` | §7 errors and panics | standards review of 3e5212d / L1-fold |
| TopologyDelta is a cloneable, fold-agnostic authorization value, so a transition checked against one state can be applied repeatedly or to a different fold. — `TopologyDelta` carries no identity for the fold or state against which it was checked and derives `Clone`. An owned clone can therefore reach `apply_delta` after the original has changed state, or reach another fold, without `plan_transition` checking that actual state. | `src/topology/fold.rs` | `808e780eab7938f964495c41b98077f58e5717751293183b41c16a26a0e1c8ad` | §3 Rust-native design principles | standards review of 3e5212d / L1-fold |
| FoldError::StaleIncarnation renders a Display message with a trailing period. — The final literal segment is `it.`, with no interpolation following it, so every rendered `StaleIncarnation` message ends in a period. None of the named automated mechanisms checks this display-text requirement. | `src/topology/fold.rs` | `85c9561540247e9021e38241a8036211fa005e2f80afe34fb4951e1663900510` | §7 errors and panics | standards review of 3e5212d / L1-fold |
| The public is_poisoned, started, and registry accessors have no rustdoc stating their contracts. — These three public methods have no attached `///` documentation. Their pre-start and poisoning semantics are part of the fold API but are not stated on the individual public entries. | `src/topology/fold.rs` | `bcdc330110a11139d77f2be887d0ed42a2f452a5aa06db26b2a07dc7ac122c09` | §13 documentation and observability | standards review of 3e5212d / L1-fold |
| The public lease query methods `holds`, `lineage`, and `lineages` have no rustdoc contracts. — All three methods are public, yet none has an attached `///` contract. Their names alone do not document distinctions such as lineage storage versus ordinary held entries. | `src/topology/leases.rs` | `056eb516940e0617fb239f2b889020011e4e683d9156413d160f058a37b34e1a` | §13 documentation and observability | standards review of 3e5212d / L3-topology-rest |
| The public `GitPath::as_str` accessor has no rustdoc contract. — The accessor is public but has no attached `///` documentation. The type-level prose does not provide item rustdoc describing this accessor’s borrowed representation. | `src/topology/paths.rs` | `6eedffd451b9d4d35e7d7c4331cdf201b6ec178eb1d536045f6672493fe88178` | §13 documentation and observability | standards review of 3e5212d / L3-topology-rest |
| `GitPath` can be deserialized or constructed with arbitrary strings despite its documented relative Git-path invariant. — Transparent deserialization constructs `GitPath` without checking relativity, separators, absolute prefixes, or traversal components, and the public tuple field bypasses validation as well. Persisted malformed path data therefore becomes domain state rather than a refusal or `RepoWide` classification. | `src/topology/paths.rs` | `b20f6e8f8338b1c4b321d42cf9b447ba5779f9570bcf254f37485b3d8cab8caf` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L3-topology-rest |
| `CandidateQueue` exposes an unconditional append operation with no queue bound or overload outcome. — The queue is a growable `Vec`, and every `push` succeeds without consulting capacity or returning an overload signal. Its API consequently defines neither a bound nor backpressure behavior. | `src/topology/queue.rs` | `1bda1e86a269bb8bbd1dbfb27a44a7e269ca4a4aa629e52f32391c2cd970e316` | §10 concurrency and async code | standards review of 3e5212d / L3-topology-rest |
| The public `QueueEntry::key` and `QueueEntry::generation` accessors have no rustdoc contracts. — Both methods are public and lack attached `///` documentation. Their relationship to the nested candidate identity is therefore not stated in their item rustdoc. | `src/topology/queue.rs` | `c1e179d6e1a347fe85e601f8b24187b45b9ef913920d529f8bcd0c784ef4f515` | §13 documentation and observability | standards review of 3e5212d / L3-topology-rest |
| The public registry query methods `len`, `is_empty`, `entries`, and `get` have no rustdoc contracts. — Each shown method is public and lacks attached `///` documentation. Their ordering and dense-key lookup semantics are therefore absent from their item rustdoc. | `src/topology/registry.rs` | `0486b5b0f56ec86c1d5b18f86d6a4d6c35fcc246f26776e533281a3f0d68eeed` | §13 documentation and observability | standards review of 3e5212d / L3-topology-rest |
| `RunFixture::drop` silently discards cleanup failure without an explicitly best-effort contract or observable failure-path test. — The cleanup result is assigned to `_` and produces no observable outcome. No explicit best-effort designation or cleanup-failure test accompanies this path. | `src/topology/registry.rs` | `12a78e6f374e6625a0bbdbc538c0087197fd19f664ba0876c9bdc14dacf795bc` | §7 errors and panics | standards review of 3e5212d / L3-topology-rest |
| The registry freezes execution and review bindings independently of `allowed_agents`, without establishing that each binding names a probed agent. — Ladder and reviewer identities from the persisted run record are copied separately from the probed-agent allow-list. No membership relation is validated before those identities enter the authenticated registry and can confer execution authority. | `src/topology/registry.rs` | `5c2e9cc42df7a895a12d80109a5eccfa8671b746d68f294f33884530689fcca1` | §14 security and trust boundaries | standards review of 3e5212d / L3-topology-rest |
| The public `register` operation accepts a freely constructible `TaskEntry` and mutates both indexes without preserving the registry’s dense-key and unique-display invariants. — The method explicitly delegates all checks to a particular caller, but its public signature carries no validated-entry distinction. Other callers can append a mismatched key, overwrite an existing display mapping, or register an invalid ladder through the supported API. | `src/topology/registry.rs` | `6105504ff3c44ef619f64b27453702babb8576fa84a07cfc8607819b1e8d562d` | §5 types, modules, and APIs | standards review of 3e5212d / L3-topology-rest |
| Original-registry derivation accepts a persisted ladder whose tiers are not in escalation order. — A `RunStarted` chain is persisted input, and this exercised constructor accepts `Mid, Frontier, Small` as a complete ladder. The recorded order contradicts the ladder’s escalation-order invariant but is retained as authenticated domain state. | `src/topology/registry.rs` | `83fb976a2f35ab263ad554335939e5b9726c3d4cb1683cf14f4fd2f17ee6bfd7` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L3-topology-rest |
| Header probing erases both UTF-8 and JSON source errors into an untyped detail string. — Both concrete errors are converted with `to_string()` into the same `FirstLineUnreadable` payload. The resulting `SchemaRefusal` has no structured source or error chain, so callers lose the error kind and its typed diagnostic context. | `src/topology/schema.rs` | `4ceb2f12c825a7e2dfaf47f3fb951df945acca55cc37c45a745196edc18fd281` | §7 errors and panics | standards review of 3e5212d / L3-topology-rest |
| The public `SchemaRefusal::NotAnUpgrade` variant has no rustdoc contract. — The public variant has no attached `///` documentation. Its `#[error]` attribute supplies a Display implementation, not rustdoc stating the variant’s contract. | `src/topology/schema.rs` | `6e2379905bc0208152c06e329363d9fa714d97452747d5092ae301d7a2c01c67` | §13 documentation and observability | standards review of 3e5212d / L3-topology-rest |
| Header probing parses an arbitrarily long first line without an input bound. — The newline search has no maximum offset before the entire line is handed to Serde, which allocates the event string while parsing. A persisted header can therefore drive unbounded parsing and allocation through this boundary. | `src/topology/schema.rs` | `786f228b59d098baf3b18d67403866d2608bf90ed95fc5256464a5895b642b7b` | §14 security and trust boundaries | standards review of 3e5212d / L3-topology-rest |
| Reader selection classifies schema 0 as a legacy schema even though this module defines only schemas 1–3 as legacy. — Any schema at or below the ceiling other than schema 4 falls through to `ReaderSelection::Legacy`, including zero. A malformed persisted schema therefore selects a reader instead of being refused at the parsing boundary. | `src/topology/schema.rs` | `a6e2a194901a8c3b06287cdc5d4e5e7088502e47c45326fbc7882b271aaabea6` | §8 filesystems, persistence, and paths | standards review of 3e5212d / L3-topology-rest |
| The `NoCommittedHeader` error Display text has a trailing period. — The derived message’s final literal ends in `recover.`. That punctuation is part of the resulting `Display` output and directly contradicts the required message form. | `src/topology/schema.rs` | `cfc480af3fbd3c1edda537ee19bd51a1dca872a9a632e6e505040b9a10b5e555` | §7 errors and panics | standards review of 3e5212d / L3-topology-rest |

### W10.3 — the whole-tree sweep's work-list

Same salvage rule. These rows belong to the sweep slice that follows the pass.

| Observation | File | Region sha256 | Standards section cited | Raised by / round |
|---|---|---|---|---|
| `extract_version` accepts tokens whose third dotted component is empty or non-numeric as valid versions. — The predicate validates only the first two components and merely checks that a third component exists. Inputs such as `1.2.error` and `1.2.` therefore become reported capability versions instead of being rejected as ambiguous external output. | `src/agent/bin.rs` | `64ad311a4eb3c45d089558a70242cd6ee44c5d48d493ff9b497b6e457e34f777` | §9 processes and external tools | standards review of 3e5212d / T5a-agent |
| The Windows batch-shim test uses a persistent PID-named directory instead of a uniquely owned RAII temporary directory. — PID reuse can expose the test to artifacts from an earlier process, and no guard owns the directory. The function also never removes it, so a passing run and every early assertion failure leave test state behind. | `src/agent/bin.rs` | `9949ac0dcb6018ab6eea892e0a9ffa50735e2b78538d2727e61aac7c7a7a8c07` | §12 tests | standards review of 3e5212d / T5a-agent |
| The live Claude probe passes the default suite when its required vendor CLI is absent. — The test is neither ignored nor otherwise classified outside the default suite. A missing executable takes an ordinary successful return, so the automated test result claims coverage without exercising the probe. | `src/agent/claude.rs` | `1f54f3090c814b087a30a264c38e182454b51b280f89d2d18e09427eecb54e33` | §12 tests | standards review of 3e5212d / T5a-agent |
| The settings-file path is lossily converted before being passed to Claude as the file to open. — `settings_path` is a `PathBuf` whose exact identity controls the permission settings used by the child. On a non-Unicode path this conversion names a different file instead of preserving the path or refusing it. | `src/agent/claude.rs` | `24d7d9bca499c7a1f269d9bbbf4d0d52cbf33bb0093eb7b1a590c187f71e66a6` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5a-agent |
| Any JSON value from a zero-exit Claude process is treated as successful unless it explicitly contains `is_error: true`. — Missing or wrongly typed `is_error` becomes `false`, and success requires no result type, success subtype, or final result field. Consequently `{}` with exit code zero authorizes `Completed`, despite proving none of the expected protocol. | `src/agent/claude.rs` | `6863f142c19e70ed04d092e734d5e78636cbcb8bf6d645efebf30c210df25cf4` | §9 processes and external tools | standards review of 3e5212d / T5a-agent |
| Unvalidated profile arguments are appended after Claude's permission controls and can widen or replace them. — A profile can append another permission-mode or settings-source option, or a skip-all option, without this effect boundary rejecting it. The raw vector therefore bypasses the adapter's otherwise narrow permission construction. | `src/agent/claude.rs` | `c0d93996435df37df79e76c527234611004a78d1406147d0d14a78bed42d33ee` | §14 security and trust boundaries | standards review of 3e5212d / T5a-agent |
| The documentation calls Claude's adapter-level settings filtering a sandbox without an enforced OS boundary. — The cited mechanism is the union or exclusion of Claude settings and permission rules, not an OS containment primitive. Elsewhere this file itself acknowledges that repository code run by an allowed gate can bypass these path rules, so the sandbox label overstates the boundary. | `src/agent/claude.rs` | `fd9c878ac6e2520541a4f52b3d876028d36080775511d99e5cdb437626446127` | §14 security and trust boundaries | standards review of 3e5212d / T5a-agent |
| The live Codex probe passes the default suite when the required vendor CLI is absent. — The test is not classified with `#[ignore = "…"]` or placed in a separate integration target. A machine lacking Codex takes the successful return and records no execution evidence for the claimed live probe. | `src/agent/codex.rs` | `0c36d6fc1bc949126ca4605c067adddb11a5f97de23d6aeab96a6f428b75d74a` | §12 tests | standards review of 3e5212d / T5a-agent |
| Raw profile arguments can override or bypass the Codex sandbox selected by the adapter. — `extra_args` is appended after the adapter's `--sandbox` setting without rejecting capability-widening options. It can therefore carry the dangerous bypass flags this file says must never be passed, or a second sandbox option, across the process boundary. | `src/agent/codex.rs` | `2bbeac184dacaba7b9bbe23cf62819380025c04bb92db21dc6f769e408e3a136` | §14 security and trust boundaries | standards review of 3e5212d / T5a-agent |
| A zero Codex exit is accepted as complete without requiring any recognized success event. — Earlier parsing skips malformed lines and ignores unknown event types, but this branch consults only the exit code. Empty output, wholly malformed JSONL, or a stream lacking `turn.completed` and an agent message can therefore become `Completed`. | `src/agent/codex.rs` | `61c2eccb8aa5ab3ced67fc600959f21a7a86d3251db55a13272d5b5757f07ec6` | §9 processes and external tools | standards review of 3e5212d / T5a-agent |
| The missing-schema probe lossily converts its schema path into the subprocess argument. — The path identifies the exact schema file whose absence is the probe's oracle, and the temporary-directory prefix can be non-Unicode. Lossy conversion can name a different path and make that different absence appear to validate the intended parser guard. | `src/agent/codex.rs` | `66babef3611e762e0de67235123a378cc0a884b8b336526be6ad3504a1991320` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5a-agent |
| Token totals from untrusted Codex events are accumulated with unchecked addition. — Two valid JSON events can each report values whose sum exceeds `u64`, causing the enabled release overflow checks to panic. The code selects no checked, saturating, or error-returning behavior for this externally supplied quantity. | `src/agent/codex.rs` | `74f12d3d4457ec13b3d5d913a6fc0cdf02ecdfbaab187c9ff64bed9f53060e32` | §5 types, modules, and APIs | standards review of 3e5212d / T5a-agent |
| The live Copilot probe passes the default suite when its required vendor CLI is absent. — Printing a hidden-by-default test message does not classify the test outside the suite. The successful return allows CI to report the test passed without executing the integration it names. | `src/agent/copilot.rs` | `35f19235c6f4fab67542cc42a115e62d1a50c97320bd4db3a7493fec893da547` | §12 tests | standards review of 3e5212d / T5a-agent |
| Copilot profile arguments are appended after the permission allow-list without capability validation. — `extra_args` can directly add options such as `--allow-all`, `--allow-url`, or `--add-dir` after the narrow permission arguments. This effect boundary therefore trusts a raw internal vector to preserve the filesystem and network limits it claims. | `src/agent/copilot.rs` | `d7c4aff24fcc243820ea3e2d5b4987900edf494381f0915b06b0220c0027c2fc` | §14 security and trust boundaries | standards review of 3e5212d / T5a-agent |
| Rate-limit classification uses unbounded substring matches that cannot distinguish unrelated failures containing the same text. — A failure mentioning an identifier or count containing `429`, or discussing a premium request for another reason, is classified as pool exhaustion. That classification changes ladder and capacity behavior, so a substring collision is not a harmless diagnostic approximation. | `src/agent/mod.rs` | `5392f8d0011e2a331837a07b17ddec9d2bc23c10ca55417f96b9d466089bfd31` | §3 Rust-native design principles | standards review of 3e5212d / T5a-agent |
| The probe-ordinal use-site census scans raw source substrings without the required syntax blanking. — It excludes only lines whose trimmed form starts with `//` or `*`, leaving inline comments and every literal form visible to the match. A string containing `probe_ordinal::NAME` is therefore indistinguishable from a production use site. | `src/agent/mod.rs` | `8f06496823361b0421067c727f22b309b2a6ba1bd160e68de54b2ddcbe7417de` | §12 tests — instruments and censuses | standards review of 3e5212d / T5a-agent |
| The process-wide-state census removes only whole-line comments and scans literals as Rust structure. — Filtering lines beginning with `//` neither handles block or inline comments nor any literal form. Joining the retained lines also changes positions and output length, allowing source text inside literals to create false findings or mask the claimed domain. | `src/agent/mod.rs` | `a8d1c83ae559aba567e7773d1dacf13372751d547504588d1987de64746f0ee3` | §12 tests — instruments and censuses | standards review of 3e5212d / T5a-agent |
| The `AgentAdapter::build` error documentation still says that this data-only method locates binaries. — The assigned adapters now return a spec containing a bare CLI name, while the runner boundary performs resolution during execution. `build` has no runner and cannot produce the documented location failure. | `src/agent/mod.rs` | `a999cb5e66c55dd566b4ca9d03b688038f7017971b9fc81f75f6aea650652d26` | §4 formatting, naming, and readability | standards review of 3e5212d / T5a-agent |
| Container-listing setup failures are represented as an empty trusted listing. — Pipe and fork failures both return zero, which the reclamation caller interprets identically to “no labeled containers remain.” This releases cleanup without distinguishing unavailable evidence from a confirmed empty set. | `src/agent/proc.rs` | `0274545064af52f7fd61c51870e95689e325445767dee0417578de16a3142fad` | §14 security and trust boundaries | standards review of 3e5212d / T5a-agent |
| A pipe-read error is silently treated as ordinary EOF. — `Err(_)` shares the EOF arm, with no flag or diagnostic telling adapters that captured output may be truncated. A zero-exit child can consequently be authorized from an incomplete transcript without any defined best-effort observability. | `src/agent/proc.rs` | `32363621d13c90b68375b4aadc67f65b0be26d6098c89d33c8d0b383e0aab586` | §7 errors and panics | standards review of 3e5212d / T5a-agent |
| The Unix wait-status helper performs multiple unsafe operations without immediately adjacent `SAFETY:` proofs. — Zero-initializing `siginfo_t`, passing its pointer and flags to `waitid`, and later reading it all require initialization, pointer, and syscall-result obligations. None of the cited unsafe expressions has the mandated adjacent proof; this is the review-only safety-proof half, not the automated `unsafe_op_in_unsafe_fn` check. | `src/agent/proc.rs` | `540e142b54c8ff0e9e6cce2fe6a4513541ee1977e428c42b218d67f48c6e7f37` | §11 unsafe and platform-specific code | standards review of 3e5212d / T5a-agent |
| Unix group cleanup retries forever when the process scanner cannot produce conclusive evidence. — `None` can never satisfy this loop and there is no deadline, retry classification, or cancellation check. Because the supervising parent waits for the reaper acknowledgement, scanner unavailability can extend a subprocess operation beyond its hard timeout indefinitely. | `src/agent/proc.rs` | `8ad13a697281764be8212e0a70084fdbfd283e083532c0346d810f62dba1f640` | §7 errors and panics | standards review of 3e5212d / T5a-agent |
| The Unix signal-monitor thread is deliberately detached immediately after startup. — Dropping the handle makes a later monitor panic or unexpected exit invisible while process supervision continues to rely on it. No owner can join it or translate its failure into a defined refusal or shutdown outcome. | `src/agent/proc.rs` | `8c2508879c0e0cefc533cfdc00f63e72daba8b4d36017a7dc111007a1b0b29f2` | §10 concurrency and async code | standards review of 3e5212d / T5a-agent |
| The Unix containment-gate census searches raw source lines and literals instead of a syntax-blanked domain. — The scanner searches every raw line for a substring, including the test's own `CALL` string and any comments or literals in the source. It then infers structure from the nearest textual attribute, so non-code text can manufacture or redirect the claimed gate association. | `src/agent/proc.rs` | `99230947929f5784de4d1203f7df543b2f4cc0e0e794cb110be19e601c2a72ba` | §12 tests — instruments and censuses | standards review of 3e5212d / T5a-agent |
| Failure to render a new reaper's container scope is silently converted into no cleanup scope. — Rendering repeats executable resolution and can fail after the scope was initially armed, but `.ok()` erases that required-cleanup failure. The reaper then starts without container reclamation and exposes no diagnostic or other failure observation. | `src/agent/proc.rs` | `a1b3a3f66a2d33a23b521d6dd8c247c2e4d6d752cd1295e1eab139c06d69f445` | §7 errors and panics | standards review of 3e5212d / T5a-agent |
| `Drain::collect` detaches its pipe-reader thread when EOF does not arrive within the grace period. — The handle is joined only when already finished; otherwise returning drops the live `JoinHandle` and detaches the reader. Its later panic or failure has no owner and cannot become a defined subprocess outcome. | `src/agent/proc.rs` | `cbe8e0bf6d1784ec6796d583d7c036030c0a586b5343a2efdc823c4887f3ba9f` | §10 concurrency and async code | standards review of 3e5212d / T5a-agent |
| Signal-handler installation can return after the guard and monitor have started without rolling those resources or earlier handlers back. — Each `?` may fail after preceding handlers were installed, while the already-detached monitor retains the state containing the guard. The error is memoized, leaving those process-wide side effects alive with no reachable cleanup owner. | `src/agent/proc.rs` | `ec6201fcac4826a9bbcb56a3a813586fe01363301a403f1b9baa2460e0a41039` | §6 ownership, mutation, and resources | standards review of 3e5212d / T5a-agent |
| The test scratch helper returns a bare path and has no RAII cleanup owner. — PID-and-tag naming reduces collisions but does not remove the directory on success, error, or panic. The pre-test deletion is not cleanup for the resource created by the current test. | `src/answer.rs` | `209bc6659563efbf1e968d2086f5c8e3dd693fcf5c19c86bc96331be6ec65883` | §12 tests | standards review of 3e5212d / T6-root-rest |
| The persisted question record is read without any size ceiling before parsing. — Persisted run data is explicitly a trust-boundary input. `read_to_string` can allocate for the entire file before its JSON schema is checked. | `src/answer.rs` | `45835e784adf82df84f8751cd7d5eacfa70d3ab7de7774e20d9702aa9247ba48` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| Answer exclusivity is implemented as a check followed later by an overwriting publish. — Two answer commands can both observe `record.answer` as empty before either answer is ingested. Both then publish, allowing the later rename to replace the first answer despite the stated apply-once policy. | `src/answer.rs` | `a47d5a9abbf1596ce76f03637df79715a596c29c49a0f38656d87224cb0975b7` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| `Pool` publicly exposes fields whose numeric and cross-field invariants are otherwise validated at the configuration boundary. — Callers can directly install non-finite or out-of-range margins, invalid allowances, and kind-inconsistent window state. This bypasses the validation performed by the configuration parser. | `src/capacity.rs` | `6cd10036c7509c13e6609551d6c2dfe6957bab8870f184f16e03199dadcbadde` | §5 types, modules, and APIs | standards review of 3e5212d / T6-root-rest |
| The public `AtMost(f64)` variant does not enforce its documented finite `0.0..=1.0` invariant. — Any caller can construct `AtMost` with NaN, infinity, a negative value, or a value above one. The documentation promises a clamped fraction, but the representation does not preserve that promise. | `src/capacity.rs` | `ec35b53d21ef9e15fb053c9dd803b0455d76b87e78a3457f9179263334ae7cc8` | §3 design principles | standards review of 3e5212d / T6-root-rest |
| A misspelled routing role is ignored, silently restoring the default execution chain. — Routing kinds choose attempt tiers and capacity expenditure. Ignoring an unknown key can weaken an intended tier floor without preventing execution. | `src/config.rs` | `6135bba6e61bfea039fb6997189e84c2f16ed1b6136d57952b3f4da8ae56198c` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| Configuration and plan snapshots read the complete file with no maximum size. — `fs::read` allocates from the size of configuration or candidate-plan input before parsing. A regular file can therefore force arbitrary memory growth, and a special file can prevent termination. | `src/config.rs` | `90879debbe84067c47efa157341f307aa159e03ee2636966ac9ddd2e3e74733f` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| An unknown configured shell silently selects the native shell instead of failing closed. — Shell selection controls how gate command text is interpreted and executed. Falling back after a malformed value can run that text under semantics the operator did not select. | `src/config.rs` | `976f72f5a20caec50755a097f9ed686ea49c2facd2f4d9d9d760ed3e77953998` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| A container-side absolute path is represented as an undifferentiated UTF-8 `String`. — Being interpreted by another boundary does not make the value cease to be a path or justify an unvalidated string representation. It needs a native or dedicated target-path type with explicit platform semantics. | `src/config.rs` | `adaa2a015a344725bd6103a767e8779c4e7b9db2b436bd942c76b8e4d9bf52a2` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| Configuration tests create process-scoped scratch files without an RAII cleanup owner. — The helper returns only a path and never removes the directory it creates. Setup failure or a later test panic leaves persistent state that later runs can observe. | `src/config.rs` | `c71256b8619bd920f7b172433a62c748e4fa807ba83658eab878ef361d108b07` | §12 tests | standards review of 3e5212d / T6-root-rest |
| TOML integers are converted to `f64` with a potentially lossy `as` cast and no range proof. — TOML integers are `i64`, and values outside the exact IEEE-754 integer range lose precision. The later positivity and finiteness checks do not establish exact representability. | `src/config.rs` | `efc48e6479432f5eb2bc98dd3b648df9d77ef3fc5aff7919f47e56d83d2b1e40` | §5 types, modules, and APIs | standards review of 3e5212d / T6-root-rest |
| Connect tests use manually managed scratch paths instead of an RAII temporary directory. — The helper deletes old state only before creating the new fixture and returns a plain `PathBuf`. Nothing removes the current test's directory on success or panic. | `src/connect.rs` | `04bb4b3fc96b3d9005c8a34cc7aa3bb1d084751c25ab5154e3e49cb23b4c29a8` | §12 tests | standards review of 3e5212d / T6-root-rest |
| A default unit test depends on an installed vendor CLI and silently passes when it is absent. — This ordinary `#[test]` probes the real `claude` binary through PATH. The early return converts the missing prerequisite into a green test instead of classifying it outside the default suite. | `src/connect.rs` | `05c2e8c5dd1617617ddad7fb7eda59f8ddd92a89e3ac0c7169a6093330f511d7` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Every error reading an existing pools file is collapsed into apparent absence. — `.ok()` treats permission errors, directories, invalid UTF-8, and transient failures exactly like a missing file. With `--force`, that permits rewriting without carrying the operator-only keys the comment says must be preserved. | `src/connect.rs` | `54b28a4aeb15257d713be661e50c207582ce7187e285f1759e36049b65e924bd` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| Operator-provided TOML strings are interpolated into quoted output without TOML escaping. — Valid parsed values containing quotes, backslashes, or multiline content can produce malformed or structurally altered TOML. The persisted configuration is written before ensuring these strings can be represented by this handwritten serializer. | `src/connect.rs` | `6a2c3ed71e2dd23653b591cef49e4fb2b8eb4df0404e0027bd9e71b7a04828f3` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| The pools-file conflict check and plain replacement are neither exclusive nor atomic. — Another process can change the file after the comparison and before `write_text`, causing its update to be overwritten. The generic `fs::write`-based helper can also expose truncation after interruption. | `src/connect.rs` | `bf07d4e933453c4833c245ca1af50f8175987600573f42f8136c8d5314af917a` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| Preserved allowances accept non-positive or non-finite floats and use a lossy integer cast. — Unlike the main configuration parser, this path installs parsed values directly into the domain type. Negative, infinite, or NaN floats survive, while large integers can silently change value during conversion. | `src/connect.rs` | `f378bec34e193b6e862210826c8ed30775bf828fca0aab815e1a1c0dafbd4b67` | §5 types, modules, and APIs | standards review of 3e5212d / T6-root-rest |
| Malformed existing TOML is silently converted into an empty operator-key map. — The comment declares leniency but defines no warning or other observable failure result. A force rewrite can consequently erase valid operator-only information elsewhere in a partially malformed file. | `src/connect.rs` | `fe8bf7801a487e4674e800f48100bfd34faed8b15e136c74124f872ad18f0a65` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| The test-presence census treats any `#[test]` within the preceding 400 bytes as attached to the target function. — An unrelated test followed closely by an ordinary helper with the required name is accepted as the required test. The existing plain-function control contains no preceding test attribute, so it does not exercise this false-positive shape. | `src/effects/tests.rs` | `4f41846421771b8fe1d2df37f863eac632cb267d28ac2eea6faae33ef7c905b5` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| The shared source walker silently omits any directory that cannot be read. — A read error returns from only that subtree while the later coarse file-count floor can still pass. Consequently every census using `scanned_sources` can report success over an incomplete domain. | `src/effects/tests.rs` | `54b90a3287b3628ad7504da63c61f1524f9b379eaf359e8fb5254d8306ea817c` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| The effects-test scratch directories have no RAII cleanup owner. — Cleanup occurs only before the directory is used, and the helper returns an unguarded `PathBuf`. Success, early return, and panic all leave the directory and its generated artifacts behind. | `src/effects/tests.rs` | `d9a9121d9c9bd4c9ac3e6888ee74f5a225f160da7368794f95ee4d72716f0b6f` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| The wildcard-arm source census has no injected violating fixture. — The test scans only the currently compliant checkout and uses a count merely to guard against a vacuous domain. Its comment describes a historical mutation, but the test never injects a wildcard arm and observes this scanner reject it. | `src/effects/tests.rs` | `faa2c093994fd4a85f1efed391c747f6334bef4c111682b590f326ea1b31daf8` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| Reviewer artifact loading silently converts every read or decoding failure into absent evidence. — The `.ok()?` treats not-found, permission denial, transient I/O, and invalid UTF-8 identically. The reviewer consequently receives an incomplete evidence set without an error or diagnostic, and the assigned tests do not exercise this failure path. | `src/engine/attempt.rs` | `839ff21a2a486e229d5335df0cb18446519504875344b8e2b695825a4ec88d7d` | §7 Errors and panics | standards review of 3e5212d / T3-engine-core |
| Worker prompt materialization reads each agent-authored artifact to EOF without a byte bound. — Artifacts are model-produced input and therefore cross the standard’s trust boundary. `read_to_string` allocates the complete file and then copies its contents into the prompt without checking a maximum size first. | `src/engine/attempt.rs` | `cb3d56bc5b8fe6115290435c1980c308137fab26c69f275dcfd978f82e85333c` | §14 Security and trust boundaries | standards review of 3e5212d / T3-engine-core |
| An attempt error can leave uncommitted agent edits while the cleanup failure is silently discarded. — `discard_uncommitted` is fallible, but its result is dropped and the original attempt error is returned unchanged. That contradicts the adjacent guarantee that a half-staged workspace is not handed back, and no warning or combined error identifies failed cleanup. | `src/engine/coordinator.rs` | `39761b05bc2d55502ce02b5a3d7059fe4b84d6929f41d3e8a383c81937b86b5b` | §7 Errors and panics | standards review of 3e5212d / T3-engine-core |
| The operational private-directory path is persisted through a lossy UTF-8 conversion. — `private_dir` is not diagnostic text: `resume.rs` reconstructs a `PathBuf` from it and uses that path for subsequent run data. A non-UTF-8 path therefore cannot round-trip and may cause resume to create or use a different directory. | `src/engine/coordinator.rs` | `8acdb449f3727b64b01830780848085cfc2dd6337f57f8850596987adde44fe2` | §8 Filesystems, persistence, and paths | standards review of 3e5212d / T3-engine-core |
| Failure to remove a never-started run directory is silently discarded. — Both fallible removals are ignored while only the earlier opening error reaches the caller. A failed public-directory removal can leave the exact husk that shadows `latest_run`, but no warning reports that outcome and the corresponding test exercises only successful cleanup. | `src/engine/coordinator.rs` | `8b8543b390e2604e145d8d4d95c6937802274b3da19d78eaeecc2758dc2e1def` | §7 Errors and panics | standards review of 3e5212d / T3-engine-core |
| A failed best-effort partial-report write is discarded without observability or a failure-path test. — The caller receives only the drain error even if writing the promised partial report also fails. The existing partial-report test proves successful writing after an append error, but it cannot inject and observe failure of this write. | `src/engine/coordinator.rs` | `fe7dfce1df7cf7d850fdec186c1cdd17a6e0728f582ec8027c1e450a1f8d0b30` | §7 Errors and panics | standards review of 3e5212d / T3-engine-core |
| Plan and configuration paths are converted into lossy strings for a record that later uses them as filesystem identities. — The returned string is stored in `run_started`, and resume joins it back onto the repository root to locate the plan or configuration. Non-UTF-8 path bytes are therefore replaced rather than preserved, so a valid original path cannot reliably be resumed. | `src/engine/preflight.rs` | `7457dc657efbf5e960a6159b0e66ec45deaa77389c4c90ffbd17aeed1001994c` | §8 Filesystems, persistence, and paths | standards review of 3e5212d / T3-engine-core |
| Persisted gate records are converted directly into executable gates solely because they have the internal `GateSummary` shape. — `recorded.gates` originates in mutable persisted run data, which the standard declares untrusted. This branch warns if it differs from current configuration but then grants its command text process authority without establishing the record’s provenance, allowing a valid-looking substituted record to become a later resume’s shell command. | `src/engine/preflight.rs` | `d344daeaca56d3fe8dabc75875dde49e379196f1bd48feee0a61942b84addf68` | §14 Security and trust boundaries | standards review of 3e5212d / T3-engine-core |
| The public topological-order API treats an unknown dependency as satisfied instead of requiring validated graph state or returning failure. — `topo_order` accepts the weak `Plan` representation directly. When a dependency ID is absent, `is_none_or` marks the task ready and returns a plausible successful order, so the API neither carries a validated graph type nor exposes the invalid state to its caller. | `src/engine/report.rs` | `7eedcc628c376c4ef95a38141773b3726f289b5f88b7ff28aa7fd11eaa96699f` | §3 Rust-native design principles | standards review of 3e5212d / T3-engine-core |
| `TaskRunStatus` deliberately anticipates future variants but remains exhaustively matchable by downstream Rust callers. — The `Unknown` variant’s documentation explicitly says the public enum is expected to receive statuses from newer versions. `#[serde(other)]` protects deserialization only; without `#[non_exhaustive]`, adding a Rust variant still breaks downstream exhaustive matches, and no rationale addresses that API-level closure. | `src/engine/report.rs` | `ef64df8b2f87e0d938015bd6f54585b72281ce272d548e6c81ade5a352ec201e` | §5 Types, modules, and APIs | standards review of 3e5212d / T3-engine-core |
| The facade census has neither an exact domain-boundary assertion nor an injected violating positive control. — The length-ratio assertion only says that an unspecified majority of the file survived; it does not pin the production boundary. The test then applies its oracle only to the live source and never injects a forbidden export to prove that the census detects one. | `src/engine/tests.rs` | `016dcbc5577d2a5fed9e54773c632004f5db77b879edfc2233ec00f1d5adc7f0` | §12 Instruments and censuses | standards review of 3e5212d / T3-engine-core |
| The default unit-test fixture resolves `git` through ambient `PATH`. — This central helper launches the bare program name `"git"`, so most engine tests depend on the host’s PATH contents and installed Git. These are ordinary unit tests in the default suite, not explicitly classified integration tests. | `src/engine/tests.rs` | `baafdd696230a69db0efd38ac1fa42d72311fd4e750b1396ac7e38a013166f6c` | §12 Tests | standards review of 3e5212d / T3-engine-core |
| The shared repository fixture uses a PID-derived raw `PathBuf` rather than a unique RAII-owned temporary directory. — The helper returns only a `PathBuf`, so no guard removes the repository on success, panic, or early return. PID reuse can encounter stale residue, and the ignored pre-test removal error lets such residue survive into a test while `create_dir_all` still succeeds. | `src/engine/tests.rs` | `be70330584deb01325819dfece5ca3d1718975a92b85118518fb029d17d05260` | §12 Tests | standards review of 3e5212d / T3-engine-core |
| The public-entry-point census scans raw Rust source without blanking comments or literals. — Unlike the earlier facade census, this helper feeds `include_str!` directly into line-based matching. A multiline string or raw-string decoy whose embedded line begins `pub fn ` would be counted as an entry point even though it is not Rust structure. | `src/engine/tests.rs` | `f9a1377250f729b49a783ac2f141edbc312dc802c632e99979df1cbf8b2cda68` | §12 Instruments and censuses | standards review of 3e5212d / T3-engine-core |
| `UnknownPinnedModel` has `Display` text ending in a period. — The format string ends with `upstroke.`. This directly violates the repository's error-text convention. | `src/error.rs` | `0d7049d7fdc78f481be54bcdac147892025717af6f5ca1f4653062d99f751070` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| The shared I/O variant labels every operation as a read. — This same variant is used for directory creation and file writes in the assigned modules. Those failures are therefore reported as “failed to read,” losing the operation context needed to diagnose or respond correctly. | `src/error.rs` | `1786e5e4cbaac06b9729868c12c679fa24e996d5b80de5cfa6fb6122d8f436b9` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| Directory durability is silently skipped when the log is addressed by a bare relative path. — `Path::new("events.jsonl").parent()` is the empty path, which this branch filters out before returning success. Creation or torn-tail truncation can therefore claim successful directory synchronization without performing it. | `src/events/log.rs` | `26655c75328f1bd3c9ec5c31e72bbc59029f858e2717c67e9e4793d25950c3e1` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| `LogTail::poll` turns every file-open failure into an empty successful poll. — The catch-all pattern does not inspect `ErrorKind`, despite the method documenting `UpstrokeError::Io` when the log cannot be read. A permission or transient I/O failure is therefore indistinguishable from “no new events.” | `src/events/log.rs` | `454c303f4fc5caa095b6ee291e2a100770ac6cc3e661dd739d71c15aa39aadb9` | §7 errors and panics | standards review of 3e5212d / T5b-events-effects |
| A committed blank or whitespace-only record is silently ignored instead of classified as corruption. — The writer never emits blank records, and the newline marks the record as committed. Skipping such a line lets a rewritten log replay successfully despite containing a committed record that is not an event. | `src/events/log.rs` | `5279b52da8f8dd603622099030968c0577f5584713843febd214c7a66de4e658` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| Whole-log reads deliberately accept an attacker-controlled file size as their only allocation bound. — Persisted run data is a named trust boundary, but this reader states that a log of any size is read in full. Bounding a read by the untrusted file's own length provides no policy maximum on allocation or parsing work. | `src/events/log.rs` | `8393c05d59aa844266bf58b1042357d00ea452502ca801bfb15806864f4cd06d` | §14 security and trust boundaries | standards review of 3e5212d / T5b-events-effects |
| `BarrierError` renders with a trailing period. — The format string ends in `resumable.` and is the error's direct `Display` implementation. Panic-policy lints do not enforce this error-text contract. | `src/events/log.rs` | `d81538e186512d47646ddd26baac3a262fed606c961d06a0ec46609c5e477de6` | §7 errors and panics | standards review of 3e5212d / T5b-events-effects |
| Incremental polling performs an unbounded `read_to_end` on the growing event log. — The buffer has no maximum derived from product policy or even the length already sampled. A large or continuously appended persisted log can force unbounded allocation and keep the poll from reaching EOF. | `src/events/log.rs` | `f0ee41b971eb758a4b4bd11335a9abb56361aaa68d97405bd6c37f5021652041` | §14 security and trust boundaries | standards review of 3e5212d / T5b-events-effects |
| The incremental reader treats truncation of committed history as a normal restart. — When the observed file shrinks, the code resets the offset and later accepts the shortened file's events. A committed-history rewrite should be surfaced as corruption, not silently substituted for the state the follower had already observed. | `src/events/log.rs` | `f2e590477513b77247c18897525363d94d21125b83c300d8139e818680843389` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| `skip_existing` collapses every metadata error into an offset of zero. — Permission, transient I/O, and malformed-path failures all become “start from the beginning,” and the API cannot report what happened. No best-effort contract or failure observability accompanies this conversion. | `src/events/log.rs` | `f9913a0807d40247b39c11d4d6cc6864d0a0135ba57578df449882b25f5fed50` | §7 errors and panics | standards review of 3e5212d / T5b-events-effects |
| The log tests maintain a second implementation of the production-region boundary. — `crate::effects::production_region` is already the shared authority, while this local copy independently finds the first test marker. The implementations also differ because this one searches whatever partially stripped text its caller supplies. | `src/events/log/tests.rs` | `14d9f54c37c0587710cf7202d6bebbcaffd6407259a81913525dec63b90c3795` | §3 Rust-native design principles | standards review of 3e5212d / T5b-events-effects |
| Event-funnel tests create scratch directories without RAII cleanup. — The atomic suffix helps distinguish cells but does not provide a drop guard. Cleanup runs only before use, so successful tests and unwinding both leave their directories behind. | `src/events/log/tests.rs` | `2374196d9b48cde6c5dab2748323a62f9f428780da512da72602ff911c23eded` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| Two source censuses use a stripper that removes only `//` comments. — Block comments and every required literal form remain visible to the primitive and branch substring searches. The implementation also deletes rather than position-preserving blanks text, contrary to the same instrument rule. | `src/events/log/tests.rs` | `f8d576b717873989630f488680d4e84cbf17f4995eaad6c749dc3ff99251ce9d` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| Schema validation decides attempt success from only `failure`, bypassing the authoritative review-outcome predicate. — `AttemptRecord::is_successful` explicitly treats `Failed` or `Unavailable` review outcomes as failure even when `failure` is `None`. This validator instead treats such a record as successful, allowing it to carry a prepared commit and close with `task_committed`. | `src/events/mod.rs` | `2339465d53d3e5b4090ef9b0be75cf2618ac593e8b40fe31118a692a8d52c39a` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| The inline event tests create scratch directories without RAII cleanup. — The helper returns only a `PathBuf`, and cleanup happens before rather than when ownership ends. Test success, early return, or panic therefore leaves state in the ambient temporary directory. | `src/events/mod.rs` | `730b24ee6f0c983718a40d7b081d734000a18356dc3f6ccc89b76d8df772f06a` | §12 tests | standards review of 3e5212d / T5b-events-effects |
| Persisted plan and configuration paths are represented as UTF-8 strings. — These fields bind a run to filesystem objects rather than merely displaying them. The representation cannot faithfully carry a supported-platform path containing non-UTF-8 data. | `src/events/mod.rs` | `79943ec1d17090f103234dd55c98d71f0a193f9c6deae4de7aac6017d71cee20` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| Event routing stores task identity as a raw `String` despite the domain having a task-ID type. — The event envelope can accept arbitrary text and mix task identity with other string fields. Using the domain `TaskId` would preserve the serialized string shape while enforcing identity semantics at construction and replay. | `src/events/mod.rs` | `7afa0abedc34712d1d549aac0383fb3ed8200ef06754aedfbb89d8f472da0786` | §5 types, modules, and APIs | standards review of 3e5212d / T5b-events-effects |
| The event timestamp is an unvalidated interchangeable `String`. — Deserialization can construct an `Event` with any string, even though replay and diagnostics treat the field as a timestamp. A validated timestamp newtype would preserve the wire representation while excluding malformed states. | `src/events/mod.rs` | `c279cce64bb18afbe5e0b9c90cc81cfa6c86062f5408128a3ff4f45c3fbf4a85` | §5 types, modules, and APIs | standards review of 3e5212d / T5b-events-effects |
| An event naming an unknown task is silently discarded during replay. — A corrupted or forged task identifier returns from `apply` without an error or observable record. Because `apply` returns no `Result`, replay cannot distinguish this malformed transition from a valid no-op. | `src/events/mod.rs` | `c74cc41127905f3199b9dcdce2890a2dde5a5e9be48f140ed2e840db2893ff8e` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| Replay accepts `run_started` anywhere in the event sequence instead of requiring it to open the log. — `find_map` accepts a valid-looking start after earlier state transitions. `replay` then applies those preceding events, allowing a rewritten log to derive state before its identity and schema boundary. | `src/events/mod.rs` | `d68effb21db9a942ebc28e5a4386913f881d68d5e91dd3c385e319bb642f903d` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T5b-events-effects |
| A public library export API returns `anyhow::Result`. — `src/export.rs` is part of the library surface, not the binary reporting edge. Its callers cannot distinguish JSON serialization failures from output I/O failures through a typed contract. | `src/export.rs` | `c76664bc273e0870fb4477e871e9ca83cb699dbdb161575db037e7d0b2018c8d` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| Export loads an entire persisted normalized plan without a size limit. — Persisted run data is untrusted even when produced by an earlier upstroke version. `std::fs::read` allocates the whole plan before digest or schema validation. | `src/export.rs` | `e27e3d0f18aee9d028085a88682f3e7d33a030d722a867b8de871182fa5760bb` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| Internally derived gates are constructed as shell command strings instead of separate program and arguments. — The user-authored-gate exception does not apply to these engine-authored defaults. Passing constants such as `cargo check --all-targets` through `ShellGate.cmd` makes the shell parse internally constructed command text. | `src/gates.rs` | `3efd556cda528240bf40032b428d1de0968404a99c4a8131deafca7763f4f065` | §9 processes and external tools | standards review of 3e5212d / T6-root-rest |
| Gate stdout and stderr are copied verbatim into persisted logs without secret redaction. — Repository-controlled gate commands can print credentials or environment-derived secrets. Output limiting constrains size but does not satisfy the separate redaction requirement. | `src/gates.rs` | `5d260823f238be70e7d4f287bea7d66210c71b76d86b1e5b08a7a948cc82384f` | §9 processes and external tools | standards review of 3e5212d / T6-root-rest |
| The shared gate-test scratch helper has no RAII cleanup owner. — It returns a `PathBuf` after manually deleting prior state. Directories from the current run remain after success and survive panics. | `src/gates.rs` | `6ac1f221f81a9dac72374e081bacc30860da49f4c358ef0b980b66a913ffcd47` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Failure to persist a passing gate's non-empty log is silently ignored. — `write_note` is surfaced only inside the `!passed` branch. When the gate passed, a log-write failure disappears and `run_all` returns success without the promised evidence. | `src/gates.rs` | `923e2929bf7674225298440f71dc17e73a6690705b2854a19ed1034d1929405f` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| A malformed `package.json` silently derives an empty gate set. — Gate derivation is verification policy, so malformed evidence must not remove all verification. The nested success-only parse falls through to `Vec::new()` without an error or warning. | `src/gates.rs` | `9402f2355591aaa1d79c35de8f20bc4801de0420e1e812621a45f7d2fec5c178` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| A default gate-resolution test assumes a PATH-resolved `git` executable. — The first assertion fails or changes behavior according to the host PATH. The test is an ordinary unit test and does not inject an executable resolver. | `src/gates.rs` | `963f929b523f9dfac66cabc08441babd566302eabd556a579579c48b80166767` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Executable suffix probing reconstructs path identity through a lossy display string. — `base.display()` can replace non-Unicode bytes before the extension is appended. The resulting `PathBuf` may name a different path or fail to find a valid executable. | `src/gates.rs` | `d682103593b563ec45b2effd05cb3b9c441d458189cbaa7d50cd61bc33324520` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| A poisoned wait-budget mutex is converted into the legitimate `Unanswered` domain outcome. — Poisoning indicates a prior panic while the shared state was held, not that no answer arrived. Returning `Unanswered` parks the task while hiding the synchronization failure. | `src/interaction.rs` | `478836bb6d2d6ae586f0aa2cd19f74049f71a2d0baf7d53e8cc7b06645dc30c3` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| The wait-budget mutex remains locked across an arbitrary, potentially long `Sleeper` callback. — `remaining` is a `MutexGuard` and is used after `self.sleeper.sleep(wait)`, so the guard spans the callback. A configured wait can last minutes, making the critical section neither small nor isolated from external behavior. | `src/interaction.rs` | `53a7b84026f2bdc929b09c500a908295a4916a0899075486edce051c54254ad5` | §10 concurrency and async code | standards review of 3e5212d / T6-root-rest |
| The answer-channel test assumes detached stdin and uses a process-global fixed temporary path. — Its expected channel changes when the harness has a terminal, so terminal attachment is part of the oracle but is not injected. The fixed `upstroke-answers-for` path is also shared across processes and has no RAII cleanup. | `src/interaction.rs` | `742835c11db890b064d2763cb08551f48282a5d4e45d7dfbba0b9e6a5c1d80fb` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Failure to flush the interactive prompt is discarded before blocking for input. — A flush failure can leave the prompt invisible while the command waits for an answer. The operation is not marked best-effort, produces no diagnostic, and has no injectable failure path. | `src/interaction.rs` | `922fde1176a0f7e53fa9e7b858a317768f7dac0409ba00149e9ed1f26d3136ae` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| The validated task representation exposes every graph and routing field for unrestricted mutation. — After validation, callers can change IDs, dependencies, tier bounds, and artifact wiring without rerunning graph checks. The representation therefore does not carry the invariants established at the input boundary. | `src/ir.rs` | `6c74127865d6ff0cfcf9163453223cfa3183d81135a56c10c1b6e67ac0feacd8` | §5 types, modules, and APIs | standards review of 3e5212d / T6-root-rest |
| Task path hints are carried as interchangeable strings rather than a dedicated path-pattern type. — These values drive glob-based blast-radius routing but have no type distinguishing them from titles, acceptance text, or other strings. A validated `PathHint` or path-pattern newtype could preserve their parsing and matching contract. | `src/ir.rs` | `ccf00b80188aa142d5fe9f85495a8532eff32903b57787814b13d94c5069fa8d` | §5 types, modules, and APIs | standards review of 3e5212d / T6-root-rest |
| The failure-kind source census scans raw Rust text without the required literal/comment blanker. — It slices and line-filters the original `include_str!` contents directly. It also has no nonzero blanked-region assertion, so prose or literal text can influence the claimed structural domain without detection. | `src/ladder.rs` | `27c85d4a3bc5b7c15b45d3c09a4a79b30aee3ace8471dac29df719734c643cae` | §12 instruments and censuses | standards review of 3e5212d / T6-root-rest |
| The Windows test manages its temporary record file with paired deletion calls instead of RAII. — Any failure between creation and the second `remove_file` leaves the record behind. The bare path has no owner whose drop runs during panic unwinding. | `src/main.rs` | `72ad0b2b668403985085aafb4c504dbb06a4f32109df25012794957498eb93cf` | §6 ownership, mutation, and resources | standards review of 3e5212d / T6-root-rest |
| The CLI source census deletes selected line-comment suffixes but does not blank all Rust literal forms or preserve source length. — Joining shortened lines changes offsets and output length, while ordinary strings, raw strings, character literals, block comments, and byte strings remain searchable. The resulting substring counts are not a conforming Rust-structure census. | `src/main.rs` | `7efab55813d15fb881343ccb782825b1d0bc4e7e144f31d11ef41f0c34ba95a9` | §12 instruments and censuses | standards review of 3e5212d / T6-root-rest |
| Malformed routing annotations are converted into heuristic or absent routing controls. — `kind`, `tier`, and especially binding `min` values influence which model and capacity execute the task. Warning and ignoring a malformed value can lower the resulting chain while still authorizing execution. | `src/plan/markdown.rs` | `b10db25a0a7bc4fa637980830032adc3e42f0477f90d48ba161c9c6b607fe3bc` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| `PlanAdapter` has no documented behavioral contract for sniffing, errors, or side effects. — The trait-level comment only identifies the design role and implementation count. It does not define recognition precedence, whether `sniff` and parsing are pure, or the error and warning obligations future implementations must share. | `src/plan/mod.rs` | `5091701b578e16d07662d44010acb665ef1c6c667df5841b67d118b74d787fb1` | §3 design principles | standards review of 3e5212d / T6-root-rest |
| Review acceptance treats a successful generic `fs::write` as a durable transcript. — `util::write_text` performs neither file synchronization nor directory synchronization. Nevertheless, this path accepts the verdict whenever that helper returns success and explicitly describes the rejected case as lacking a durable transcript. | `src/review.rs` | `076df1686bec71a736faa97258a3e73200bcba633c1107f872113b2a0cfd3a8d` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| Agent-authored artifacts are appended to the review prompt without per-artifact or aggregate bounds. — Only the diff has a size ceiling; this loop copies every artifact into a growing `String`. An artifact set can therefore force arbitrary allocation and model-input work before the reviewer process is spawned. | `src/review.rs` | `5a49b5b22765c7cdec765ce2bc9b6f764a01592782b9a1e7c8fe69a41cbdcfcb` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| The deadline helper silently succeeds outside its special environment and uses real sleeping as the timeout oracle. — In the default suite the missing environment variable produces a green no-op test. When invoked as a helper, wall-clock sleep and process scheduling determine whether the surrounding deadline assertion passes. | `src/review.rs` | `945766929113dfb4b0761876e06a791e3ac344b62986b4d52f732a4465520dfc` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Review configuration tests create persistent process-scoped scratch directories without RAII. — `scratch_config` returns the parsed value and drops the only path without removing its directory. Panics and successful test completion both leave the fixture behind. | `src/review.rs` | `97a2fbbbd8868561c134f916d80966c43b4d963fd7146b2c1026432d3a45dfd4` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Reviewer output can approve while carrying required changes or malformed/coerced verdict fields. — Once `pass` is a Boolean, the parser constructs a verdict without checking that a pass has no required changes or that `needs_human` is compatible. The helper also defaults or coerces list fields, so malformed success can become authoritative. | `src/review.rs` | `aa0b1c9f710f603bc74464baa8c07740575795dc53777653938eda0ddf5f138c` | §9 processes and external tools | standards review of 3e5212d / T6-root-rest |
| The supposedly hermetic routing fixture stores a bare temporary path in a process-lifetime `OnceLock`. — `OnceLock` owns only two `PathBuf` values and cannot remove the directory when tests unwind or finish. The process-scoped name prevents some collisions but does not provide cleanup. | `src/route.rs` | `410082d93b5ba95cd69c8cb16ceb6356c2e4bca5c3641781abfa4d74b959e81d` | §12 tests | standards review of 3e5212d / T6-root-rest |
| `snapshot_tree` silently produces partial snapshots when directory entries or file contents cannot be read. — Directory errors are skipped, `flatten` drops entry errors, and file-read errors omit the affected files. Tests comparing these maps for byte-identical state can therefore pass even when the state was never fully observed. | `src/rundir.rs` | `1cc68a3a61a95dcaefaf7894147c5f91af18f924407cdd66120fcf53219f1a20` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| `RunPaths` accepts an unvalidated path-shaped run ID that can escape both authorized roots. — Absolute run IDs replace the preceding path during `join`, while `..` components traverse it. The resulting paths are later passed directly to `create_hooked`, so constructing this public value can confer filesystem creation authority outside both run roots. | `src/rundir.rs` | `1cf78971da127c87f6515b4ddd5afa486c72d69c354f76c71fba6f6cedba0397` | §14 security and trust boundaries | standards review of 3e5212d / T4-workspace |
| The question-payload write boundary interpolates an unvalidated component into an authoritative path. — An absolute component makes the formatted filename absolute, and separator or `..` components can leave the questions directory. The public effect function validates neither a single-component grammar nor containment before writing. | `src/rundir.rs` | `1e5f413d6388eaee5594a0021c64d13c297671a22cd7b74a34c46b135af94557` | §14 security and trust boundaries | standards review of 3e5212d / T4-workspace |
| The private-half ownership proof derives the run identity from a lossy rendering of the public directory name. — Distinct non-UTF-8 filenames can map to the same replacement-character string. This value is compared with persisted ownership fields and placed in the deletion proof, making a diagnostic rendering part of an authorization decision. | `src/rundir.rs` | `4244f6cb88e22deeb2dba0b691a1429f77a290e64d2d5d0d06c53d581088fc4e` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T4-workspace |
| `read_dir_names` converts every enumeration failure into an empty directory and discards entry errors. — A directory-level error returns an ordinary empty vector, while `flatten` omits individual failures. This helper feeds both ownership classification and public-husk deletion, so unavailable evidence can be interpreted as proof that no content exists. | `src/rundir.rs` | `4addfa7a46a20fcebfae0db428e0dd15fbdecb0349f86cb5d08faef071ab0786` | §7 errors and panics | standards review of 3e5212d / T4-workspace |
| The publication kill regression accepts a panic or ordinary error path as proof of process abort. — The helper executes `unreachable!` after ignoring the publication result, so replacing `Injection::Kill` with an error leaves the staged file unpublished and then exits unsuccessfully by panic. This status assertion and the subsequent filesystem assertions all accept that non-aborting mutation. | `src/rundir.rs` | `5fb0cb6535a717b55af9f301541692639db568983baddcdac98611ac513784d3` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| The durability test uses a process-global lower-bound counter whose result depends on concurrently running tests. — Other tests can increment `barriers_performed` between this test's before and after reads. Their increments can satisfy `performed >= claimed` even when one or more of this test's own barriers are removed, so scheduling affects whether the mutation is detected. | `src/rundir.rs` | `61e7ba5a5175d4ffa42d9bf8f210af712969e8bd868312c00e1325f761658108` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| The answer staging boundary uses an unvalidated component as part of its write path. — `format` permits absolute paths, separators, and traversal components to flow directly into `Path::join`. A caller can consequently redirect this public write outside the answers directory without crossing any validation or containment check. | `src/rundir.rs` | `975a3033271821f91216179bae82995fe4a1380a19697e62d18d213aff009859` | §14 security and trust boundaries | standards review of 3e5212d / T4-workspace |
| The rundir test helper returns a manually cleaned PID-named directory without RAII ownership. — The directory is removed only before use, and a bare `PathBuf` cannot clean it during panic or early return. PID reuse can also cause a later process using the same tag to collide with or delete stale fixture state. | `src/rundir.rs` | `bd1c629ff25fef2788627331eb70db36a834c1a80da200f8901e6f482280b8bf` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| `CreatingMarker` represents the canonical private-directory identity as a UTF-8 `String`. — This field is later reconstructed as a path and participates in the deletion ownership proof, so it is identity rather than diagnostic text. A `String` schema cannot represent valid non-UTF-8 paths and forces the proof to assume or lose platform-native path data. | `src/rundir.rs` | `d2a7611a7d6dfada252424283a0f76c664f1a97d4d97d5a0f3476cff664ccdde` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T4-workspace |
| The private-half ownership proof falls back to an uncanonicalized public path when canonicalization evidence is unavailable. — A canonicalization error is replaced with the caller-provided lexical path instead of retaining the husk. If the owner record contains that lexical lossy string and the other fields agree, the proof can still mint the token accepted by private-half deletion. | `src/rundir.rs` | `eb0463a6ae63dc8b69740dc416acec8f611e6915a313a72dd9c4216b9947b8c4` | §14 security and trust boundaries | standards review of 3e5212d / T4-workspace |
| Every unrecognized container status is accepted as proof that the container exited. — The wildcard includes malformed output and future or unexpected daemon states, not just known terminal states. Treating all of them as `Exited` permits removal and admission to proceed without affirmative termination evidence. | `src/runner/container.rs` | `18481d92a7857b33f3885629e58b71f8d805e0ba3bfe9bfc677e13474ea6f827` | §9 Processes and external tools | standards review of 3e5212d / T1a-container-prod |
| Bind-mount source identity is constructed through a lossy string conversion inside Docker’s comma-delimited mount syntax. — The lossy result is not diagnostic text; it selects the host directory Docker mounts. Non-UTF-8 paths can collapse to another spelling, while commas in valid paths are also left unescaped in the `--mount` mini-language. | `src/runner/container.rs` | `4bc9dc8ff20a65eb61b4c9f0addeaf7a18a081fb9710c54e76e6f8f478ed78d1` | §8 Filesystems, persistence, and paths | standards review of 3e5212d / T1a-container-prod |
| A malformed Docker exit-code response is silently converted into the legitimate signalled-process state. — Parsing failure and a genuinely absent exit code require different caller actions, but `.ok()` collapses them both to `None`. This operation is not documented as best-effort and the malformed output is not observable. | `src/runner/container.rs` | `5787ce8102515562850c662949375af065abc7656cb9502e77f9de1f1852a9b3` | §7 Errors and panics | standards review of 3e5212d / T1a-container-prod |
| The free launch path returns from mount or create failures without releasing resources already created. — The intent is durable before the mount call, and the view may exist before the create call. Either `?` can therefore leave an intent or view behind without invoking `cancel_reached`; only the later image-mismatch branch performs cleanup. | `src/runner/container.rs` | `5da2143f9e9616cf35844e0c31563aa89d34f4eabaa721325842ec7fd98acb3f` | §6 Ownership, mutation, and resources | standards review of 3e5212d / T1a-container-prod |
| Image-inspection output silently treats structurally missing digest or tag fields as empty fields. — The fixed Docker template defines three fields, but missing second or third fields are accepted through `unwrap_or_default`. A truncated or structurally malformed success response consequently becomes a valid `ImageInspectionRaw`. | `src/runner/container.rs` | `9afdfe6b9df11b021bc661bb7d2e2ee5905695ec4479950c10cd3356aafcfecb` | §9 Processes and external tools | standards review of 3e5212d / T1a-container-prod |
| The container launch transition has two production implementations. — This free `launch` implements the same intent/view/create/start transition as `ContainerRunner::launch` in `exec.rs`. The two paths already have different cleanup semantics and therefore can drift independently. | `src/runner/container.rs` | `b0558d6048702544dec6f27689c9191b70c51c3b8063917b96be8d2b8cc413f0` | §3 Rust-native design principles | standards review of 3e5212d / T1a-container-prod |
| The Docker subprocess primitive has no timeout or cancellation protocol and captures both streams without a pre-allocation bound. — `Command::output` can wait indefinitely and allocates complete stdout and stderr buffers before returning. The runner’s later truncation operates only after these unbounded vectors already exist, so it does not bound subprocess output capture. | `src/runner/container.rs` | `ff4901e9342fd3439bac03d7cbc56975f3fff9049209c1eac6a33190b24032d6` | §9 Processes and external tools | standards review of 3e5212d / T1a-container-prod |
| Reconciliation accepts missing ownership labels and checks only run and incarnation when intent and daemon evidence coexist. — A missing label is silently skipped, and `LABEL_RUN_DIR` and `LABEL_INVOCATION` are never compared with the intent. The census can therefore perform its liveness and reclaim decision despite incomplete or contradictory daemon evidence. | `src/runner/container/census.rs` | `ee97ca5a01b5fa9b27944f73a4aa4be0e55b152d3271a55b82720f10bc5ad99a` | §14 Security and trust boundaries | standards review of 3e5212d / T1a-container-prod |
| The shared scratch helper returns an unmanaged directory and provides no RAII cleanup. — The directory survives normal completion, panic, or early return unless an individual caller happens to remove it manually. Pre-removing a PID/thread-derived path does not provide a drop guard and can also reuse stale state when removal fails. | `src/runner/container/census/tests.rs` | `3496673ebd6d2f6807e14352a45d4c1bc15faae43f8cf1b2f0a3819498234fff` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The protected-view test reports success when its permission premise cannot be constructed. — Root or a filesystem that ignores the mode reaches this plain `return`, so the default suite records the contract as tested. The branch neither fails diagnostically nor classifies the test outside the default suite. | `src/runner/container/census/tests.rs` | `3519835435d4f7ad9549131876fb79be2f7fde10b33d26493599c58dd8bb6d0f` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The test named as proving a refusal instead requires successful completion and an untouched report. — An error from the census fails this region because it calls `expect`. The function name therefore describes the opposite outcome from the one its oracle accepts. | `src/runner/container/census/tests.rs` | `35e4b5ebff4cf68332af47947d6745439ae0575e983aa183d4a288ea6f6752b1` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The lock-file source scan uses a presence sentinel instead of injecting and detecting a forbidden access. — The `is_running` assertion only establishes that some expected source remains. A broken scan that always reports all four forbidden needles absent still passes because no violation is injected through the same detector. | `src/runner/container/census/tests.rs` | `6373693846d9a5b1cc59bdd2ef5f4a37e8a7c08dae78cda6eb71d5ed34a87e3f` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The cross-role race oracle accepts a fully serialized execution and never forces the disputed interleaving. — If one racer reclaims all four containers and the other later reports zero, `total >= names.len()` passes. The assertion checks both roles eventually ran, not that their reclaim steps overlapped. | `src/runner/container/census/tests.rs` | `79c506b9bc374da9f0fd99a8c2b4e96085a8915bc7143b8e3b46517576d791f0` | §10 concurrency and async code | standards review of 3e5212d / T1b-container-tests |
| The no-volume source scan has no injected forbidden operation as its positive control. — This region only observes the current production source and succeeds when none of three substrings occur. An empty or otherwise defective scan can report the same absence because no planted volume operation is required to fail. | `src/runner/container/census/tests.rs` | `7d3bbf72f79c9cd08fbbb4222e2b397a6689d5d4fcb2fd3cd8ef716d5e550b84` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The concurrency test fails or passes according to whether the scheduler happens to interleave at least one round. — Releasing two threads at a barrier does not prevent either thread from completing before the other runs. The final assertion therefore rejects a valid serialized schedule even though the following test already provides a controlled interleaving. | `src/runner/container/census/tests.rs` | `8c8852950c69c622c71c5b007fd64dafb2e363c7a282c1143f5856662a6b24a1` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The documentation still says `concurrent_reclaimers_converge` races two fresh censuses after that test was changed to race a resume and a fresh census. — The referenced test now constructs one `"resuming incarnation"` and one `"foreign write command"`. This stale explanation misstates the coverage gap and the reason for the following regression test. | `src/runner/container/census/tests.rs` | `abd29de37a22370e1543199207e26b254e738d6b572e6156314a9af42aab57ce` | §4 formatting, naming, and readability | standards review of 3e5212d / T1b-container-tests |
| The test name says it races one container, but the fixture creates four containers. — The `0..4` domain is deliberate and materially changes the possible interleavings and oracle. A failure name claiming one container therefore does not state the sentence the test executes. | `src/runner/container/census/tests.rs` | `be127d3526e790bf0112934d64fbd5f03f27caf182ef1fa2c48ce3ec3c9da336` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The real-Docker test returns a passing result when Docker is unavailable. — The reason equality can validate the helper's message, but the Rust test harness still records this branch as success. The test is an ordinary default `#[test]`, not an ignored or separately classified integration target. | `src/runner/container/census/tests.rs` | `cd7727ababb4fc214ba15e6896041a876fcc7f24825873274cebdf11609726bd` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The persistent-`PermissionDenied` test accepts any error even though its seeded bytes are independently malformed. — Under a privileged user or filesystem that ignores the permission bits, reading `b"{}"` succeeds and parsing the incomplete record still returns an error. The generic `is_err` assertion therefore does not prove that persistent `PermissionDenied` exhausted the retry path. | `src/runner/container/census/tests.rs` | `e6cc767cc9a3300272a267ab396ad109c2e22f5efab1bc6411a8d40206aba2a9` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The credential-withholding documentation incorrectly lists Review as a role refused a credential location. — `supplies_credential_location` returns true for `ExecutionRole::Review`; only Gate and the shell probe return false. The comment describes three refused roles and therefore contradicts both the implementation and its tests. | `src/runner/container/env.rs` | `2b766fc0cff9a673afcdccef25f2bac10e2a44908e737330124b608f6df49b92` | §4 Formatting, naming, and readability | standards review of 3e5212d / T1a-container-prod |
| A release failure is hidden whenever supervision or collection already failed. — Cleanup is attempted, but `outcome?` returns before `released?` is inspected. The caller therefore cannot distinguish a clean failure from one that also left container, view, or intent residue requiring operator attention. | `src/runner/container/exec.rs` | `26fbab710d9d69eb5cf62a6d50c296e28067e9f91c6124af2686f152f0e7aa72` | §3 Rust-native design principles | standards review of 3e5212d / T1a-container-prod |
| Docker-gated tests return successful completion when their runtime prerequisite is absent. — `skipped` merely asserts the expected reason and returns `()`, so the default test harness records the test as passed. The test is neither outside the default suite nor marked ignored, and no diagnostic is emitted. | `src/runner/container/exec.rs` | `332ef5f7c5b5c4cc157b08f41e70dd297360541235f14cbd5006597a3ad82fd5` | §12 Tests | standards review of 3e5212d / T1a-container-prod |
| Confinement uses a lexical prefix comparison as its entire filesystem-containment decision. — `Path::starts_with` does not resolve `..`, symlinks, reparse points, or path aliases. A mount source reaching a withheld directory through any such alias can therefore evade this check and be granted to the container. | `src/runner/container/exec.rs` | `3e0c3df5db999002cfbd6b5ebc0340735f07fa286df13d465f944a1be183e1cb` | §8 Filesystems, persistence, and paths | standards review of 3e5212d / T1a-container-prod |
| A Docker-gated test comment still says successful collection discards container stderr. — `DockerCli::collect` now calls `exec_streams` and returns stdout and stderr separately. The cited comment describes the repaired behavior as a live adapter defect and misleads readers about what the test observes. | `src/runner/container/exec.rs` | `ae9f69439dcf5c9d54a37d687847efae3452d5cb6ffcfd202f58509778c3fe45` | §4 Formatting, naming, and readability | standards review of 3e5212d / T1a-container-prod |
| The descendant-containment test uses timed polling and sleeping as its synchronization mechanism. — Whether the sampler observes both processes depends on scheduler and host timing within a twenty-second window. No barrier or channel signals that the leader and descendant have reached the required state. | `src/runner/container/exec.rs` | `ba778304e761d60e2d8ec3349e5493051ca7fdf51d22acb68c2dc7e97600d51a` | §12 Tests | standards review of 3e5212d / T1a-container-prod |
| The module documentation falsely states that `super::launch` creates the container before mounting its Git view. — The current `super::launch` implementation calls `mount_git_view` before `create_container`. This comment still describes the older, repaired order and incorrectly claims that the free function cannot work. | `src/runner/container/exec.rs` | `dfecd174399b353cbb5fbef7dcf93b7dfe21e3529911ce2763f0fcad9eb73847` | §4 Formatting, naming, and readability | standards review of 3e5212d / T1a-container-prod |
| Bare test runs all receive the same repository key, allowing concurrent suites to pre-clean each other’s live containers. — An empty scope is the normal fallback when `CARGO_TARGET_DIR` is absent, and every process then derives the identical fixed key. Container names consequently collide across concurrent bare test runs, while `preclean_names` kills by that shared name without a liveness check. | `src/runner/container/fake.rs` | `741726168b4d87cb8e10ed780d86441026d0c0af7e5dd7105d3c324d94f12f09` | §12 Tests | standards review of 3e5212d / T1a-container-prod |
| `IntentWritten::certify` mints an authority-bearing capability after syntactic JSON deserialization without validating record invariants. — The code does not validate identifiers, decode and validate `run_dir`, or prove that the record’s fields correspond to `name`. It nevertheless constructs `IntentWritten`, which authorizes container creation and start. | `src/runner/container/intent.rs` | `83afbdf814dbbcb957dba1e0a4f2392768b28bb8490f1ac375b569257b083596` | §8 Filesystems, persistence, and paths | standards review of 3e5212d / T1a-container-prod |
| The invariant-bearing encoded run-directory field is public despite requiring construction through `ContainerIntent::new`. — The documentation explicitly asks callers not to fill `run_dir` by hand, but its public visibility permits exactly that and also permits later mutation. A raw or noncanonical string can therefore bypass the encoding invariant used to select an owner’s lock. | `src/runner/container/intent.rs` | `c56b5ed1297c14c98c544f7077ccfd2f06655561fc494a5ced6cdf5580b9ad91` | §5 Types, modules, and APIs | standards review of 3e5212d / T1a-container-prod |
| Runner rebuilding returns warnings by mutating a caller-owned output vector. — The warning set is part of the rebuild result but travels through `&mut Vec<String>`. This permits warnings from separate operations to mix and makes the function’s complete outcome unavailable as one returned value. | `src/runner/container/resolve.rs` | `fc22209103a6480dbf91b5c308e65fde67d2189b0a086b2acc46332b77babec6` | §5 Types, modules, and APIs | standards review of 3e5212d / T1a-container-prod |
| The read-only-resolution source scan substitutes a nonempty-source sentinel for an injected forbidden effect. — Finding `fn resolve_container` does not demonstrate that the forbidden-name classifier reports an offender. A scan that always leaves `named` empty can satisfy both assertions. | `src/runner/container/resolve/tests.rs` | `3a4200c42f9a3e46f5aeebdd6689ffc8d0458b0de70ff3b6b67b2708c0827f27` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The resolve suite's scratch helper provides no RAII cleanup. — It returns only a `PathBuf`, so temporary repositories remain after success, panic, and early return. Discarding a pre-clean failure also permits stale contents to be reused. | `src/runner/container/resolve/tests.rs` | `47a27384950bf704ed5643971105c7937aad1fcadf14ae0e3b737a165a57da8b` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The intent-writer census populates its control only from allowed production matches rather than an injected offender. — `matched_needles` proves that each spelling currently occurs in an allowed file. It never drives the `offenders.push` branch with a planted disallowed writer, so the claimed positive control does not observe the expected failure. | `src/runner/container/resolve/tests.rs` | `e66e22360e451c39ad5dc85e45832eed260c08853c6be48f1362ac201e4f4427` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| Default unit tests discover Git through ambient `PATH`. — `Command::new("git")` resolves whatever executable the test process inherited through `PATH`. The callers are ordinary in-module `#[test]` cases, with no classification outside the default suite. | `src/runner/container/resolve/tests.rs` | `ed4ec1188a48c943c357c0186abfc903ec8507bf085adc9aa88baf5207ade245` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The purported two-reclaimer race can execute serially and still satisfy every assertion. — The barrier only releases both threads; it does not suspend either reclaimer inside the disputed sequence. `raw_answers().len() == 2` proves both eventually called `stop`, which is also true when one completes before the other begins. | `src/runner/container/tests.rs` | `04615e6460d181b1a1f06cb9aab159e7eabdee81a3127ea13a0f09a9b59fed28` | §10 concurrency and async code | standards review of 3e5212d / T1b-container-tests |
| The live removal-race test returns a passing result when none of its rounds exercises the diagnostic it claims to test. — `no_image` only asserts that its text contains the expected phrase and that a forcing variable is absent. Consequently the ordinary default suite reports success even though the disputed removal-in-progress outcome was never observed. | `src/runner/container/tests.rs` | `32f1de7b3d7ccb89e8bd382590e180c8c43c6d43223e541ee7141fda2b16c86c` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The real-Docker test deletes whatever container has a globally fixed name before proving ownership. — The name contains no process, build-slot, or unique fixture component, while the Docker daemon can be shared by concurrent test invocations. `docker.remove(name)` can therefore target another run's live container, and its error is discarded before this test creates its own. | `src/runner/container/tests.rs` | `38ef915801cc7b43b2321bafc9a7c19c39a1b31408d5f795e4c09043c15dc943` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T1b-container-tests |
| The container suite's scratch helper returns an unmanaged temporary directory. — No owner removes the directory on drop, panic, or early return. The preliminary best-effort deletion is not RAII and does not make reuse safe when it fails. | `src/runner/container/tests.rs` | `3e70b6bb62d4a5c9e72050511e307c7b8df7a3e2d1cff6eceda389d90842d174` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The removal-race fixture uses a fixed delay to assume the container has begun writing. — Elapsed time does not establish that Docker started the command or that the layer write reached the intended state. Machine load can make 1.5 seconds either insufficient or unnecessarily long, so the race premise remains scheduler-dependent. | `src/runner/container/tests.rs` | `4229f722a4a4899d79f49d9350bc61239292abb406f5afe0e05554caef21abbf` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The lint-level census uses an unscoped substring search that treats an item-level attribute as a module-level lint declaration. — Any `allow(` or `deny(` anywhere in the blanked file returns a level, although the census claims each child module states its own level. A narrow function attribute can therefore leave the inherited module policy intact while satisfying this predicate, and no reason for that weaker interpretation is recorded. | `src/runner/container/tests.rs` | `54fd0e9d70f6fb51b90273897f92d1665cd766894cbc529e138d76f8e853863a` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The Docker-gated-test census treats raw `fn name(` text as proof of a compiled `#[test]`. — This first scan operates on concatenated raw sources, so a comment or string can satisfy it. Even a real function declaration still passes after its `#[test]` attribute is removed, contradicting the diagnostic that the name is a test. | `src/runner/container/tests.rs` | `781b104404729aa858e91cb5eb8ca121f87f317db60461c935020f72d14533c9` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The protected-role-view test silently passes when its permission premise is unavailable. — Running as root or on a filesystem that ignores the mode reaches this return before the production behavior is exercised. Restoring permissions does not classify the test as skipped or provide failing evidence. | `src/runner/container/tests.rs` | `97fd80376da1fdfe4dff211fb05f628e9e5663ebcfea35031a59a4d014a48fb9` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The removal-race fixture discards failure to start the container even though a running workload is its premise. — A failed start is not best-effort for this test; it changes whether removal can overlap with the container's workload. Continuing after that error can turn broken setup into the later passing return for an unobserved race. | `src/runner/container/tests.rs` | `a4175b7e4f2afaa80623c2f0a5cb2a08c268ea4124e794a252a56600254ef44a` | §7 errors and panics | standards review of 3e5212d / T1b-container-tests |
| Default real-Docker tests return success when either Docker or a usable image is absent. — Both branches return `()` through assertion-only helpers, which the test harness records as a pass. Counting the function name elsewhere does not provide an ignored classification or executable evidence of the Docker contract. | `src/runner/container/tests.rs` | `b5fa69b290d922cc1e97e943bdbbd1c3e9209d088db9113b757d9d1588b09e1c` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| The test mutates the process-global panic hook without serialization or a guard that guarantees restoration. — Other tests can panic concurrently while the empty hook is installed. If `expect_err` itself panics, control never reaches `set_hook(hook)`, leaving the global mutation installed for the rest of the process. | `src/runner/container/tests.rs` | `bb2a19d57d3fe712cfbed23fc5d529cf9a62a07714221109e70ba51c2a51c905` | §6 ownership, mutation, and resources | standards review of 3e5212d / T1b-container-tests |
| The view-removal census uses existing authorized occurrences as its control and never injects an unauthorized removal. — The occurrence floor merely proves at least two current removals were found. No planted direct `remove_dir_all` is passed through the offender path, so an instrument that incorrectly authorizes it is not required to fail. | `src/runner/container/tests.rs` | `cfa59f8fb25de327d696948595e412e4808117ea63d6ce23f427aad01e8ede62` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The whole-tree effect census scans only the prefix before each file's first test attribute and pins the domain with a loose file-count floor. — `production_region` can omit later production items when an early `#[cfg(test)]` item occurs. `scanned > 20` still passes after such truncation or after files disappear, so it does not establish the complete domain claimed by the test. | `src/runner/container/tests.rs` | `dce464f3a4af51a46bd5ea5bd43e3d3419cccb9d0690f84a7312c565b3adedb5` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The lock-holder subprocess uses a thirty-second sleep to keep the synchronization premise alive. — The readiness file signals acquisition, but nothing signals when the parent has finished; continued lock ownership depends on the parent being scheduled within thirty seconds. A slow run can therefore lose the lock before the parent makes its observation. | `src/runner/container/tests.rs` | `f9fb3946bcff72d0eec1f0feaa23d6562be494ecb01522da149ae0ec79a366e6` | §12 tests | standards review of 3e5212d / T1b-container-tests |
| Documentation for a nonexistent comments-only blanker is attached to the unrelated `walk` function. — `walk` accepts a directory and enumerates Rust files; it neither accepts `source` nor blanks comments. The orphaned paragraphs therefore become false rustdoc for the filesystem walker. | `src/runner/container/tests.rs` | `fdcf24d13e5200c80a537ab7a1ffb9aff86ed4194e5ba06ebcb3c3f29bfd41f2` | §12 tests — instruments and censuses | standards review of 3e5212d / T1b-container-tests |
| The shared Git fixture returns an unmanaged temporary path instead of an RAII-owned directory. — The helper manually removes a predictable path, ignores that cleanup result, and returns only `PathBuf`. Assertions, early returns, or panics therefore leave fixture repositories behind for later runs to encounter. | `src/runner/container/view.rs` | `99ad9012bc54da02812d5cd1c38ea6851011c829228d9325703d9a9f75fe72a4` | §12 Tests | standards review of 3e5212d / T1a-container-prod |
| Resolving a linked worktree assumes the path stored in its `.git` file is UTF-8. — A linked worktree can reside beneath an OS-native non-UTF-8 path on Unix. `read_to_string` rejects such a valid `gitdir:` target before it can be represented as a `Path`. | `src/runner/container/view.rs` | `c19170f6f7e15971a74972314a4034cf40f1ae36d03edda8201a2d5845a2371b` | §8 Filesystems, persistence, and paths | standards review of 3e5212d / T1a-container-prod |
| Failure to read `packed-refs` is discarded and later reported as an unresolved reference. — `if let Ok` treats permission, corruption, and transient I/O failures like an absent or nonmatching file. The final Git error loses the filesystem source and prevents callers from distinguishing an unresolved ref from an unreadable repository. | `src/runner/container/view.rs` | `ee6f47a4cd18f739be8006b8655ab447f6d7002798ec2aa8fc49e8b09380499a` | §7 Errors and panics | standards review of 3e5212d / T1a-container-prod |
| The HostRunner construction census hard-codes six engine files and never proves that list is the whole engine production domain. — `SITES` is both the expectation and declared scope, while the separately hard-coded source array mirrors it. Adding another engine module creates no disagreement, and the local substring control cannot prove that an omitted file was scanned. | `src/runner/host.rs` | `2c721a2fe8088654ae090498396b9a89f8d5a937d220af01f5c17d1bc13ce77e` | §12 Instruments and censuses | standards review of 3e5212d / T2-runner-core |
| HostRunner's ordinary Debug output includes the complete ambient process environment without redaction. — `HostRunner::new` constructs this field with `HostEnvironment::from_process`, whose base contains every process-environment entry. The formatter delegates that environment to its derived `Debug` implementation, so tokens and credentials can enter ordinary diagnostics. | `src/runner/host.rs` | `38e59a9165999ee53a06c4bb4e8cfd594e07c72f20228a20abbcf47cf4ac36f4` | §14 Security and trust boundaries | standards review of 3e5212d / T2-runner-core |
| The Windows process-liveness helper uses sleeping as synchronization. — The helper waits for process death by polling and sleeping for a fixed interval between observations. Its deadline bounds the wait but does not turn the sleep into a state signal. | `src/runner/host.rs` | `41aa66b49867c80287755f67fa4c856aeb9a14c3fa32d7dd9a24668bc3ed02f3` | §12 Tests | standards review of 3e5212d / T2-runner-core |
| Shell preflight copies raw external stderr into a diagnostic without secret redaction. — The shell is an external tool running with the composed environment, so its stderr is untrusted and can contain environment-derived credentials or other sensitive material. This branch passes stderr verbatim into `UpstrokeError`, with no redaction or omission. | `src/runner/host.rs` | `64f1a72c3becca97e3d901dac2eb7c4442546708b3797d62d7c0de73f4ea4b2e` | §9 Processes and external tools | standards review of 3e5212d / T2-runner-core |
| Executable discovery checks a mutable pathname and later spawns by that pathname, creating a check-then-act race. — `is_program` reads file type and, on Unix, mode bits, but the returned value preserves only the path. Another actor can replace that directory entry before `Command` opens it, so the process is not atomically bound to the file that discovery approved. | `src/runner/host.rs` | `6ecd7df29895cd1c495d52dda6c3f34da9faad72c031281343fe5adf91f9ae4a` | §10 Concurrency and async code | standards review of 3e5212d / T2-runner-core |
| The HostRunner construction census strips only full-line slash comments instead of blanking all Rust comments and literals. — Block comments, trailing comments, and every literal kind remain available to forge the `HostRunner::new(` count. Collecting and joining selected lines also changes positions and output length rather than blanking the removed bytes. | `src/runner/host.rs` | `7f72bb026eac628491f01a0bd9a3d9828b07b9c13f6990198f74598e5f3ff4ec` | §12 Instruments and censuses | standards review of 3e5212d / T2-runner-core |
| The shared test scratch helper returns a bare PathBuf rather than an RAII-cleaned temporary directory. — The ULID makes each directory unique, but a `PathBuf` has no cleanup behavior. Callers rely on manually placed `remove_dir_all` calls, so an assertion failure or early return leaves the directory behind. | `src/runner/host.rs` | `c116680d14f014f279eed56110403d1df7939054afee12a0399ad12ecf87764b` | §12 Tests | standards review of 3e5212d / T2-runner-core |
| The public Probe variant lets callers bypass InvocationId's validated probe constructor. — Because `InvocationId` is public, callers can directly construct this variant with any `ProbeTarget`. That bypasses `InvocationId::probe` checks for empty, disallowed, or overlong agent identifiers before the value is rendered into container and intent-file names. | `src/runner/invocation.rs` | `0eb840ddaba162040f5b525078ac5d7199eee0d1a230e1e6943c6a61c24aa67b` | §5 Types, modules, and APIs | standards review of 3e5212d / T2-runner-core |
| The runner-request construction census has no positive control that injects an invalid construction through its actual scanner. — Its corpus is only `production_sources()`, which it immediately summarizes as the current live tree. The subsequent table equality and role count never demonstrate that an out-of-builder `RunnerRequest` is detected by this scanner. | `src/runner/mod.rs` | `392d17fe0cbccfcf12c27db4939e0a9c7b2824e1ee5cb67cd051030cacf1fa87` | §12 Instruments and censuses | standards review of 3e5212d / T2-runner-core |
| The reviewers-profile test validates a prose roll against type fields but never observes either caller named by its contract. — `rolled` is derived solely from the hand-maintained `ROLL` table, and the assertion guarantees only that the table has one cell per struct field. Both production callers could omit or misassign the same field while this equality remains green. | `src/runner/mod.rs` | `500f75bc5d489f01621de0502147883a7860c5cfac10c3bd78cf45096a79c14d` | §12 Tests | standards review of 3e5212d / T2-runner-core |
| The plan-authored filename census excludes unsafe sites once the identifier is rebound to an alias. — Membership in the claimed domain depends on the extracted expression retaining the literal spellings `display_id` or `task.id`. A preceding rebinding such as `let id = task.id.to_string()` removes those spellings, so a later unsafe stem is skipped rather than classified. | `src/runner/mod.rs` | `576b7789288bb83242d9d749423212711ce9bcacfc24f72690cac95c6fbdb13b` | §12 Instruments and censuses | standards review of 3e5212d / T2-runner-core |
| CommandSpec derives Debug even though it carries command environment and prompt payloads. — The type's `env` overlay can carry sensitive values and its `stdin` stores complete worker or reviewer prompts. Derived formatting emits both fields without redaction, allowing ordinary diagnostics of a command or enclosing request to expose them. | `src/runner/mod.rs` | `9bc23f2b389cac20cd4a5712bff2757e5cbee34f0349f6b5f3ccbbd3c955a2ec` | §14 Security and trust boundaries | standards review of 3e5212d / T2-runner-core |
| The WorkerProfile field census parses raw Rust source without the mandatory comment-and-literal blanking pass. — It locates braces by substring and treats every trimmed `pub ` line in the raw slice as a field. Block comments or multiline string and raw-string content can therefore forge either the selected body or its field set. | `src/runner/mod.rs` | `a80c7d675056098263b02f1b0538c7b4920046d643e27f6b99abd3c249460bee` | §12 Instruments and censuses | standards review of 3e5212d / T2-runner-core |
| RunnerRequest publicly exposes fields whose role, agent, and invocation values must satisfy a cross-field invariant. — The type exposes every component needed to construct combinations such as a gate with an agent or an implementation role with a gate identity. This bypasses the role-specific builders that the surrounding documentation identifies as the invariant-preserving construction points. | `src/runner/mod.rs` | `bc47e973ee32adfd945e7790a44abb044eb9d76a88bbde3afd508c687d30fcd1` | §5 Types, modules, and APIs | standards review of 3e5212d / T2-runner-core |
| Status loads an entire persisted normalized plan without a size ceiling. — The plan is persisted run data and is untrusted at this read boundary. It is fully allocated before digest and JSON validation. | `src/status.rs` | `3cf12066926f57f6536f82c2abcfd8bc321ce1d86f871aca601046d2d80860d1` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| The husk-status unit test depends on PATH-resolved `git` and manually managed scratch state. — The fixture cannot run without a suitable ambient `git` executable. Its directory is also managed by paired deletion calls, so panic before final cleanup leaves state behind. | `src/status.rs` | `653851d5ba3f7f0b42688543338ebd136bed586627b0d77dceac7056f340318f` | §12 tests | standards review of 3e5212d / T6-root-rest |
| `follow` discards output-writer failures and can return success after emitting a truncated stream. — A broken pipe or other writer failure is ignored for every event. The function continues polling and ultimately reports `Ok(())`, leaving the caller unable to distinguish complete output from truncation. | `src/status.rs` | `ee07077ca239f00da6b4f87862882babb5b24e7954960e34690fecd2146f63d1` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| Failure to derive the repository key is collapsed into absence of a husk answer. — `RepoKey::for_repo` returns a `Result`, but `.ok()?` maps every failure to `None`. The caller then reports the unrelated run-resolution error instead of the repository-key failure. | `src/status.rs` | `f17db54d229440cddb6e73695b25ee62190aa5eab741449ecc7d1f2b90063922` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| The collision test relies on live time and pseudo-random output rather than controlled inputs. — `ulid()` reads the real clock and advances global nonce state, while the test asserts that a probabilistic projection does not collide. No deterministic fake or mathematical boundary case controls the values under test. | `src/ulid.rs` | `68bf49214b0f55bf40db5aaa148464926df000e4d51de0a28e20a5315ea7fb5b` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Utility tests manually create scratch directories without RAII cleanup. — This fixture deletes prior state and returns no cleanup guard. It leaves the current directory behind, and any panic before a paired deletion in other tests also leaks their fixtures. | `src/util.rs` | `3a0b533879158a456518af18a289811ba954d837c6a2cfcefc0720fffbfe0b73` | §12 tests | standards review of 3e5212d / T6-root-rest |
| Failure to close the owned Windows directory handle is discarded. — `CloseHandle` has a success/failure result, but the function returns only the flush outcome. A close failure can therefore leak the resource while the API reports success. | `src/util.rs` | `515ac2ff3f7b35f477c92ff11f61968cece8e4b457110b9cc6da31df7b7541d2` | §7 errors and panics | standards review of 3e5212d / T6-root-rest |
| `read_file_bounded` uses the untrusted file's own length as its limit rather than enforcing a maximum. — A very large regular file causes `read_to_end` to grow the vector to that entire size. The metadata length makes the loop finite but is not a resource bound chosen by the application. | `src/util.rs` | `e6f40c184e3b2264679c3244d0331f12b58e2dc04720a424525f6019dd4fc68c` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| `write_text` exposes an undifferentiated truncating write with no persistence contract. — `std::fs::write` replaces in place and provides neither atomic publication nor durability. The helper's API and documentation do not state which callers may safely accept those semantics. | `src/util.rs` | `fba744f867aab705ebc97349a2faa7d3f8d192e639691614401c6811986fd0c4` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| Normalized-plan output uses a plain truncating write without defining atomicity or durability. — An interruption can leave a partial JSON document at the requested path. The method's contract does not state that this is acceptable or distinguish replacement from atomic publication. | `src/validate.rs` | `357f5fc3cfb8b72430c161d4f0fc97df916b25611487e01628fb3b2f3d202af7` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T6-root-rest |
| Cycle detection recursively descends the untrusted task graph without a depth bound. — A plan can supply a dependency chain as deep as its task count, and task count itself is uncapped. The recursive DFS can exhaust the process stack before returning a validation error. | `src/validate.rs` | `7ca326666522f26d24048de11181d7ff611ed1349a656400e7d088925dc22e7f` | §14 security and trust boundaries | standards review of 3e5212d / T6-root-rest |
| Validation tests use a bare `PathBuf` scratch helper with no RAII cleanup. — Removing old state before setup does not clean the new fixture. Success, error, and panic paths all leave the directory behind. | `src/validate.rs` | `dfb504e21b5d5cf4e85b89f8d4666868083d20f198ecf31be96f321dbeb26050` | §12 tests | standards review of 3e5212d / T6-root-rest |
| The workspace test fixture returns a manually managed PID-named temporary directory rather than an RAII-owned unique directory. — Cleanup occurs only before fixture creation, and the returned `PathBuf` has no destructor. A panic or early return leaves the repository behind, while PID reuse can make a later run delete or inherit stale test state. | `src/workspace.rs` | `07165e9f11d598c6af24b1a1fcaaf3bff7463cdfcaa0e634f57b29d50b7a8c48` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| `sync_parent` reports successful durability without syncing the parent directory on non-Unix platforms. — On the supported Windows configuration, the function discards its path and unconditionally returns success. Callers use this result to complete durable intent publication and removal, so the API certifies a guarantee it did not perform on that platform. | `src/workspace.rs` | `2348544d9613a1ad2c7afa7232e6b6fdf32663af8191d85ee51092c324d28d75` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T4-workspace |
| Gate-workspace reclamation derives deletion targets from empty intent filenames without proving which repository created them. — `create_snapshot_intent` persists no owner identity, and this code obtains the snapshot solely by stripping and validating the filename. It then passes paths derived from that name to forced cleanup; the documented requirement that callers supply the same repository is not on-disk ownership proof. | `src/workspace.rs` | `38e6261929302bc2bd12ae3a65f8bb01c29f8d6a0a0125de6201da52427ba10d` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T4-workspace |
| An error from `wait_with_output` returns before the stdin-writer thread is joined. — The `?` on the child wait precedes `writer.join`, so that error path drops the join handle and leaves the writer detached. Its write failure or panic is then unobserved, and the thread can outlive the operation. | `src/workspace.rs` | `5cf52c367cf315a6fb959a380d221a38ece970f2332fc01d2fa83142d425ceae` | §10 concurrency and async code | standards review of 3e5212d / T4-workspace |
| Gate cleanup discards directory-removal errors and then deletes the durable retry intent. — Either directory removal can fail while the function proceeds to remove the intent that makes cleanup resumable. The failures have no observability, and a successful return can leave untracked worktree or hooks residue that later reclamation no longer knows about. | `src/workspace.rs` | `8fb6cd5e8f72c64f8a420c99efe78980ade7c6d51882a5e936a063bfe728dd26` | §7 errors and panics | standards review of 3e5212d / T4-workspace |
| The post-cleanup worktree check treats malformed porcelain output as proof that no registration remains. — The parser ignores every field that does not have the expected prefix and validates no record structure, terminators, or duplicates. A malformed `worktree` field therefore produces `false`, which authorizes subsequent directory and intent removal rather than refusing ambiguous evidence. | `src/workspace.rs` | `98fca399908d794eefe7c0f41dec7600eb0448c4bd9bb7bb0bdc8a47080029b9` | §9 processes and external tools | standards review of 3e5212d / T4-workspace |
| `git_output` waits for Git without a timeout, cancellation path, or descendant cleanup. — The synchronous `output` call can wait indefinitely for Git or a descendant retaining its pipes. This integration exposes no deadline or cancellation handle with which a caller could terminate and reap the process tree. | `src/workspace.rs` | `b1fe828574ce83272f9f0cc21fca0cb1755cefc02ca430ed5af7fbf873ff4685` | §9 processes and external tools | standards review of 3e5212d / T4-workspace |
| `parent_sha` treats every unsuccessful Git status as a root commit. — `rev-parse` can fail because the object is malformed, missing, inaccessible, or because the repository itself is unhealthy. This branch converts every such status into the same `None` used for the legitimate root-commit case. | `src/workspace.rs` | `c521c8a9b83babb619ef64287bd14c01e94b116ccff0058f554ccf16d37c9e8a` | §7 errors and panics | standards review of 3e5212d / T4-workspace |
| Prepared-commit validation accepts contradictory duplicate tree, author, or committer headers. — `find_map` considers only the first occurrence of each singleton header, so a later contradictory occurrence is ignored. A persisted commit ID can identify adversarial object bytes, and matching only the first headers can incorrectly certify that object as the prepared commit. | `src/workspace.rs` | `fd7e48ba5156fe091529e93b2565ef688957816010d1ca98ecc44e1426c7400b` | §9 processes and external tools | standards review of 3e5212d / T4-workspace |
| The IdUnread kill regression accepts any child failure instead of proving that the child aborted. — The child ends in `unreachable!` if the injection stops aborting, and that panic also produces an unsuccessful status. Because the completed operation leaves the same unreferenced object state inspected later, replacing the abort with that panic can satisfy the entire test. | `src/workspace_manager.rs` | `01650b3f23edca77bbb4386893cdab3a7a619194aed20048aac5e025c4d89129` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| The public-function census scans raw Rust text without blanking comments or literals. — Splitting the unprocessed source on a textual function prefix allows comments and literals to invent functions or signatures. The instrument has no lexical blanking step or blanked-region assertion, so its claimed domain is not structurally established. | `src/workspace_manager.rs` | `2be141315809c03d581c325fbf17c894da93df1a7edaa0054c3bc882f3b480e3` | §12 instruments and censuses | standards review of 3e5212d / T4-workspace |
| `refs_under` silently omits malformed records from security-relevant Git output. — A line without a space is discarded by `filter_map`, while other malformed field shapes are accepted without validating the refname or object ID. `refuse_unexpected_refs` relies on this list, so an omitted record can make an incomplete namespace appear acceptable. | `src/workspace_manager.rs` | `306df00c03c104194484d1fd128aef95b77050efe21d2508dd4fe60d7350aa69` | §9 processes and external tools | standards review of 3e5212d / T4-workspace |
| Durable publication uses a fixed sibling temporary name shared by concurrent writers. — Every write to a given destination selects the same extension-replaced path and opens it with truncating `File::create`. Concurrent writers can therefore overwrite one another's staged bytes or rename another writer's staging file. | `src/workspace_manager.rs` | `6edd4136646dd63f107d4ff2b3bd93874c72d8bdcc98f56aa2c8962102b0ce00` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T4-workspace |
| `commit_parent` discards both Git failures and malformed output as parent absence. — The first `.ok()` erases command and exit-status errors, and the second erases invalid UTF-8. Those failures are returned as the same `None` used for a root commit, with no best-effort contract or observability. | `src/workspace_manager.rs` | `7583769eee123e00e0484ed905eb13f28dcbb63c971c488d4ea86eb2abe5f265` | §7 errors and panics | standards review of 3e5212d / T4-workspace |
| The worktree parser silently skips stray fields and lossily coerces malformed Git output. — An attribute outside a recognized record is discarded, and invalid byte sequences in attributes are replaced rather than rejected. The parser consequently cannot distinguish a valid record stream from malformed or contradictory output before its records are used for containment and cleanup decisions. | `src/workspace_manager.rs` | `9d9c67cbe253d3800084d3b4e35a6f1154845c326bd8b956a7729ef467793899` | §9 processes and external tools | standards review of 3e5212d / T4-workspace |
| Intent filenames are treated as sufficient authority to reclaim worktrees without validating their ownership records. — `IntentRecord` contains the run and incarnation identity, but `intents` never reads it and derives each slot solely from the filename. `reclaim_intents` subsequently force-removes the worktree and intent for every returned slot, while filename syntax proves no ownership. | `src/workspace_manager.rs` | `a80c3c08dbd22d1093928853ffb08a636533e25051309439d8a1c43e18b3c739` | §8 filesystems, persistence, and paths | standards review of 3e5212d / T4-workspace |
| The sampled-argv census locates functions and counts expressions through unblanked substring searches. — `split_once`, the textual closing-brace search, and `matches` all operate on comments and literals as well as Rust structure. A comment or string containing a signature, brace-shaped text, or `OsString::from(` can alter the census without changing executable code. | `src/workspace_manager.rs` | `a9ea103603825e51ea498757148aae41e597bac7e2c80f3576154b77df5ccb9d` | §12 instruments and censuses | standards review of 3e5212d / T4-workspace |
| `direct_ref_target` converts every unsuccessful `show-ref` exit into legitimate ref absence. — Git distinguishes an absent ref from fatal repository and invocation errors through its exit status. This branch maps all nonzero statuses to `None`, preventing callers from distinguishing absence from failure. | `src/workspace_manager.rs` | `b9b6a4268e59665bb6795b8736be35f3b3545fb60300e8abc056be3d4dced4f7` | §7 errors and panics | standards review of 3e5212d / T4-workspace |
| The loose-object oracle treats directory and entry read failures as a valid empty or partial observation. — Failure to enumerate the object directory returns the same empty vector as a genuinely empty store, while `flatten` silently drops individual entry errors. Tests comparing this result before and after an operation can therefore pass without having observed the object-store side effect they claim to measure. | `src/workspace_manager.rs` | `d1228b18a7e511aeacd3ed07a7d761f34ef61c743a620ae7528c1cc97bdea428` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| The non-UTF-8 filesystem regression silently passes when its filesystem fixture cannot be created. — Returning here records a successful integration test without exercising Git or the filesystem behavior named by the test. The separate pure-byte case does not provide the missing filesystem premise. | `src/workspace_manager.rs` | `d4d87d24a677535fa2ee13e421f4d61724730d6fc861599e84b7885d91924455` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| A default unit test writes its machine-varying evidence artifact into the source checkout. — The selected path is beneath `CARGO_MANIFEST_DIR`, and the same test later writes the generated histogram there. The test is neither isolated in a temporary directory nor classified outside the default suite, so it mutates shared workspace state and can collide with concurrent runs. | `src/workspace_manager.rs` | `e90f81cd0f7a09aae013bc30a1305364f3ca4724ed1a9b1367d2ecf405057b7c` | §12 testing strategy | standards review of 3e5212d / T4-workspace |
| The central Git runner has no timeout, cancellation, or descendant-process cleanup protocol. — `Command::output` blocks until Git and its inherited descendants close their output handles. This helper supplies no deadline or cancellation mechanism and cannot terminate or reap a descendant tree if Git hangs. | `src/workspace_manager.rs` | `fcfe4c903b4def9f858d22e9794ec79f0de3de68a010d452f7b7ec82e7ae604c` | §9 processes and external tools | standards review of 3e5212d / T4-workspace |

### What this review covered, and what it rejected

Fourteen lenses over `src/`, every `.rs` file assigned to exactly one lens.
Accepted 321 findings (101 layer, 220 whole-tree); rejected 0.

| Lens | Accepted |
|---|---|
| `L1-fold` | 8 |
| `L2-effects-wire` | 10 |
| `L3-topology-rest` | 16 |
| `L4a-engine-topology-prod-a` | 21 |
| `L4b-engine-topology-prod-b` | 18 |
| `L5-engine-topology-tests` | 28 |
| `T1a-container-prod` | 22 |
| `T1b-container-tests` | 30 |
| `T2-runner-core` | 14 |
| `T3-engine-core` | 14 |
| `T4-workspace` | 33 |
| `T5a-agent` | 27 |
| `T5b-events-effects` | 22 |
| `T6-root-rest` | 58 |

## From S5 rounds 5 and 6 — the sweep's remit, not this merge's

Round 5's five lenses returned **7 `standards` observations** under the shield. Three were
things this session introduced and are repaired in-slice because they are mine; four are
routed here.

| observation | section cited |
|---|---|
| **A wrapped string literal without the `\`-continuation** leaves a run of eight or more spaces in the rendered message. Three new instances in the pre-clean witnesses were repaired; the class is **tree-wide and predates this slice** — `rundir.rs:5194`, `rundir.rs:5203`, `workspace_manager.rs:5536`, `workspace_manager.rs:7015`, `effects/tests.rs:1787`, `effects/tests.rs:1800` among them. A `grep -rnP '"[^"]*\s{8,}'` over `src/` is the whole sweep | no citable section |
| **`each_census_needle_covers_the_domain_its_doc_states` has a whole-tree name for a test over two helpers** local to `runner::tests`. The name promises more than the body | no citable section |
| **`the_frozen_pool_table_is_read_through_one_seam`'s headline is unscoped and its needle is one file.** The assertion message scopes itself; the name does not | no citable section |
| **The two ending-guard witnesses use opposite idioms for one rule**, and one's doc argues against the idiom its neighbour uses | no citable section |

The two that were repaired rather than routed, because a false sentence is not a style
preference: `emit.rs`'s repaired field doc opened "the two lines **above this one**", which
resolves only against the version it replaced; and `AttemptContext::start`'s historical note
sat inside its `# Errors` section, so rustdoc rendered provenance as part of the error
contract.
