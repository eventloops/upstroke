# PR #43 — frontier review record, `8578012`

| field | value |
|---|---|
| **Verdict** | **CHANGES_REQUIRED, four findings** |
| **Reviewed SHA** | `8578012328e6e33c9596593039a256f81357f8cb` |
| Pull request | eventloops/upstroke#43, into `codex/parallelism-design` |
| Reviewer | `gpt-5.6-sol`, `model_reasoning_effort=max`, read-only sandbox |
| Transport | `codex exec` via `~/bin/review-pr.sh 43`, 90-minute per-pass limit |
| Driver knobs | `REVIEW_ROOT` (a worktree at the reviewed sha), `REVIEW_PROMPT_EXTRA` |
| CI at the reviewed SHA | GREEN on the newest run per name, selected by run id |
| Inputs | the diff, the tree at that sha, the committed prior review records, the preserved CI logs, and the pull request's title and body |

The fourth review of #43. Its second finding is the one that had survived three rounds
unseen: the macOS record conceded that changed embedded documentation alters the test
binary and the concurrent workload, and then concluded that the failure could only be
pre-existing — a conclusion its own concession refutes. Its first finding catches a guard
claimed and not delivered: the ledger said a checker enforces the deletion rule, and that
checker is seat scratch tooling that no reader of this repository can run.

## The review, verbatim

## Findings

1. **P1 — the structural deletion is still incomplete, and the claimed checker does not exist.** Outside the ledger, the body retains quantities, chronology, CI state, cross-branch state, and mutable conclusions: “one … one” and “Neither” ([pr.md](/srv/worktrees/pr43-r4/pr.md:5)), “narrowed repeatedly” ([pr.md](/srv/worktrees/pr43-r4/pr.md:16)), this PR’s CI outcome ([pr.md](/srv/worktrees/pr43-r4/pr.md:24)), another PR/branch’s state ([pr.md](/srv/worktrees/pr43-r4/pr.md:44)), and “resolves when … serialised” ([pr.md](/srv/worktrees/pr43-r4/pr.md:75)). The new ledger row nevertheless says a checker enforces number-words, conclusions, chronology, and cross-branch state ([pr.md](/srv/worktrees/pr43-r4/pr.md:141)). No such checker is added or present; the existing body validator accepts this body and checks only structure/schema. Concrete sequence: another unexplained failure is recorded → “one … one”/“Neither” becomes stale → validation still passes → the ledger falsely reports the stale-body guard as fixed.

2. **P1 — the prior non-attribution finding remains unfixed.** The macOS record acknowledges that changed `include_str!` inputs alter the test binary and can change whether the race manifests, then concludes the failure “cannot” be a behavioural regression and “can only” be pre-existing ([macOS record](/srv/worktrees/pr43-r4/reviews/2026-08-28-macos-proc-signal-single-failure.md:127), [same record](/srv/worktrees/pr43-r4/reviews/2026-08-28-macos-proc-signal-single-failure.md:136)). The body also falsely says non-attribution is stated as a command rather than a conclusion ([pr.md](/srv/worktrees/pr43-r4/pr.md:64)). Concrete sequence: changed embedded Markdown alters binary layout/test workload → the timing-sensitive race manifests → the no-`src/`-diff command still returns zero → the record dismisses a change-caused manifestation. The `PR43-ATTRIBUTION-NOT-STRUCTURAL` row’s `fixed` disposition remains too strong.

3. **P2 — the previous ledger-misread finding was recorded but not repaired.** `PR43-SCOPE-COUNT-AND-CHRONOLOGY` still says the earlier body claimed “modified once”; the committed review says it claimed “modified twice” ([pr.md](/srv/worktrees/pr43-r4/pr.md:136), [prior review](/srv/worktrees/pr43-r4/reviews/2026-08-28-pr43-frontier-review-3395396.md:38)). Its disposition still claims no chronology, contradicted by “narrowed repeatedly.” Yet `PR43-ROWS-MISREAD-THE-REVIEW` marks this fixed ([pr.md](/srv/worktrees/pr43-r4/pr.md:140)). A maintainer following the ledger receives two contradictory histories and a false fixed status.

4. **P2 — rows added this round contain wrong lineage and provenance.**

   - `PR43-TWO-MATCHING-RULES-CONTRADICTED` links to the Windows-oracle ID, but it was introduced by the repair of `PR43-MECHANISM-OVERCLAIMS-A-CAUSE` ([pr.md](/srv/worktrees/pr43-r4/pr.md:137)).
   - `PR43-ROWS-MISREAD-THE-REVIEW` is marked `introduced_by_feature`, although the bad rows were produced while repairing earlier findings.
   - `PR43-DELETION-APPLIED-TO-DIGITS-ONLY` cites `MAINTAINING.md:33`, which merely discusses recording review evidence and does not support its failure sequence. It also promotes the prior review’s P2 finding to P1 without explanation.

5. **P2 — the byte-for-byte validation claim is false.** The Windows record gives `src/engine/topology/scaffold.rs:1356` ([Windows record](/srv/worktrees/pr43-r4/reviews/2026-08-28-windows-topology-kill-single-failure.md:37)); the preserved log contains `src\engine\topology\scaffold.rs:1356:5`, likewise using backslashes and a column for the other assertion. No assertion-site normalization is disclosed. A literal matcher following the claimed byte-exact fingerprint therefore misses the actual Windows line.

No required information was lost through deletion: fingerprints and retrieval commands remain durable, while counts, history, and rollback ranges are recoverable from Git and CI. `pr.diff` exactly matches the base-to-head diff and changes only review Markdown; `src/**`, `DESIGN.md`, decisions, gates, and configuration are untouched, so the panic/`anyhow`/path rules introduce no additional finding. The contradictory matching rule, wrong `_exit` count, and Windows file count were successfully removed.

VERDICT: CHANGES_REQUIRED