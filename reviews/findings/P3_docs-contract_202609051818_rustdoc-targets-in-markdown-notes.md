---
id: PR157-ASTRA-RUSTDOC-LINKS
severity: P3
disposition: deferred
category: docs-contract
pr: 157
reviewed_sha: af2c3efe93673acf5a3ae0c849db5f7657ec5579
location: docs/internals/gates.md:38
provenance: introduced_by_feature
first_bad:
guard: Future notes-link maintenance should resolve Rust item destinations to repository files or notes anchors
---

This internal navigation finding is deferred under the owner's 2026-09-05
docs fast-track and stack stop policies. It does not require another repair
cycle or block the documentation stack.

## Failure sequence

Render `docs/internals/gates.md` as ordinary Markdown and follow the `parse`
link at line 38. Its destination is the Rustdoc symbol `ShellKind::parse`,
not a repository path or an anchor. CommonMark rendering produces
`<a href="ShellKind::parse"><code>parse</code></a>` rather than a link to
the method at `src/gates.rs:34`. A Markdown reader cannot resolve that Rust
symbol as Rustdoc could before the comment moved.

Two other added notes carry the same problem. The `HostRunner` link at
`docs/internals/engine/tests.md:2018` targets
`crate::runner::host::HostRunner`. The reference definition at
`docs/internals/config.md:374`, used at line 367, targets
`crate::topology::events::RunnerPolicy`.

The independent CommonMark rendering and parsed destinations are preserved in
`/srv/worktrees/astra-20260905/agents/astra_review_157/docs-evidence-af2c3efe.json`,
generated by exclusive buildq job `2751f23f16244be1ba3d99696c02a99e` on the
unchanged reviewed head. This demonstrates a navigation defect only.

## What the change that takes this up should do

Replace Rustdoc symbol destinations with valid relative source links or notes
anchors. Include reference-style definitions as well as inline links in that
cleanup. Preserve the displayed symbol names and the migrated contract text.
