# Review: Step 5 — gates

- **Date:** 2026-08-08
- **Scope:** commit `f9ff47c` (Step 5: gates — src/gates.rs, src/config.rs, src/engine.rs, src/error.rs, src/lib.rs)
- **Level:** max (10 finder angles → 1-vote verification → gap sweep; two findings verified empirically with probe processes)
- **Result:** 15 findings (13 correctness, 1 efficiency, 1 simplification) — **all fixed** in the follow-up commit ("Step 5.1")

| # | Finding | Where | Verdict | Fix |
|---|---------|-------|---------|-----|
| 1 | Gate-created files (Cargo.lock, target/) leak into the next task's commit; defeats empty-diff axis; dirty branch at run end | engine.rs | CONFIRMED | Post-commit `discard_uncommitted()` scrub; same primitive on the failure path |
| 2 | Test-provenance markers matched as substrings — `exit(`/`split(` pass, assertion-only test improvements fail | gates.rs | CONFIRMED | Identifier-boundary marker matching + test-path and assertion signals (lenient by design, review is backstop) |
| 3 | Missing shell binary classified as gate failure (rolls back paid work); shell never pre-flighted | gates.rs | CONFIRMED | `Gate::check` returns `Result` (spawn = environment abort, §19); `shell_available()` at pre-flight |
| 4 | `cmd /C` mangles gate commands containing double quotes (std arg quoting vs cmd.exe) | gates.rs | CONFIRMED (empirical) | `CommandExt::raw_arg` for the /C tail; round-trip test |
| 5 | Resolver hard-refuses builtins, env prefixes, quoted paths, relative scripts, .com tools | gates.rs | CONFIRMED | Shell-complex commands skipped; builtin allowlists; workspace-relative probing; PATHEXT |
| 6 | Config discovered from CWD while gates derive/run in repo_root | config.rs | CONFIRMED | `load()` takes `discover_in`; `ValidateOptions.config_root` plumbs the repo root everywhere |
| 7 | `timeout_secs = 0` accepted → every gate instantly killed as timed out | config.rs | CONFIRMED | Rejected at load with an actionable message |
| 8 | Unsanitized gate/task names in run-dir filenames; write errors swallowed (NTFS ADS, dir escape); `id=a/b` aborts run | gates.rs | CONFIRMED (empirical) | `util::filename_component()` everywhere; write failures noted in the failure summary |
| 9 | Previously-ignored `[gates]`/`[engine]` shapes now hard-fail with raw serde errors | config.rs | CONFIRMED | Sections parsed as raw values with actionable shape messages naming `[[gates]]` |
| 10 | Derived `npm test` gate fires on the always-failing npm-init placeholder | gates.rs | CONFIRMED | Placeholder script skipped in derivation |
| 11 | `--dry-run`/`validate` blind to gates (§18 says the preview shows them) | main.rs | CONFIRMED | Analysis owns gate derivation; validate renders `gates: …` and warns on unresolvable commands |
| 12 | `shell = "bash"` silently runs `sh` | gates.rs | CONFIRMED | Real `Bash` variant (`bash -c`), verified at pre-flight |
| 13 | Prompt never names the exact allowed gate commands (exact-match rules unusable) | engine.rs | CONFIRMED | Prompt lists the literal gate commands agents may run |
| 14 | Static provenance check ordered after minutes of gates | engine.rs | CONFIRMED | Hoisted above `run_all` |
| 15 | `run_all` doc promised logs for every gate; only failures wrote one, pass output discarded | gates.rs | CONFIRMED | Pass and fail logs both written (evidence trail); `GateFailure.log_tail` carries the 8 KB feedback payload |

Cut by the 15-finding cap (noted, not fixed): PowerShell 5.1 exit-code collapse in logs, duplicate gate names accepted, unknown `[[gates]]` keys silently ignored, gate time missing from task durations, `find_program` vs claude `locate()` duplication, triple temp-repo test scaffold. The `tail()` duplication and dead `FEEDBACK_TAIL_BYTES` const were fixed incidentally (shared `util` module; const now feeds `GateFailure.log_tail`).
