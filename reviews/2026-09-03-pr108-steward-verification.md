# PR #108 — steward verification, re-derived from git at the reviewed head

| field | value |
|---|---|
| **Head this record was generated at** | `fc55ee81dd3571a8bcbb6fcefa6dc9054017baa9` |
| **Base compared against** | `1cbdccd4045df5c9d80f2faae84dfdf3380957e7` |
| Derived by | `m1_steward`, a session separate from either implementor |
| Generated by | `gen-record.py`, which refuses to emit a count two engines disagree on |

**Why this file exists.** Under the owner's ruling of 2026-09-03, head-pinned
quantities live in the review record rather than in the pull request body: the
record is written once, at the reviewed head, and never re-derived, so it cannot
go stale. Every number below was computed by the generator at the head named
above, not transcribed from an earlier measurement.

**How the counts were taken.** `/usr/bin/grep` on the build box is **ugrep
7.8.4**, and its POSIX-ERE path silently under-reports on some patterns — on one
file in this very module, 28 matching lines counted as **1**, with rc=0 and no
warning. Every load-bearing count here is therefore derived **twice**, by Python
`re` and by `grep -cP` (PCRE2), and the generator exits non-zero rather than
print a figure they disagree on. Structural claims use a walk over enumerated
content rather than a regex count, because a parser cannot silently under-report.
An earlier revision of the body carried a child-declaration total that was wrong
for exactly this reason; the reviewer caught it, and this is the corrected
derivation.

## 1. Every moved body is byte-identical, in both directions

| measure | value |
|---|---|
| `fn` declarations at base | **129** |
| `fn` declarations at head | **129** (107 across the ten children, 22 in the root) |
| of those, with a body | **128** at head, 128 at base |
| bodyless | **1** — name, the `GitRefName` trait signature, bodyless at base too |
| head bodies byte-identical to a base body of the same name | **128 of 128** |
| base bodies with no byte-identical counterpart at head | **0** |

Mismatches forward: `none`. Missing in reverse: `none`.

Matching is by **name and digest together**, so a body cannot be credited against
a different function's, and the reverse direction is what rules out a body
silently dropped. Per file at head:

| file | declarations |
|---|---|
| `src/topology/fold.rs` | 22 |
| `src/topology/fold/apply.rs` | 23 |
| `src/topology/fold/check_attempt.rs` | 12 |
| `src/topology/fold/check_candidate.rs` | 2 |
| `src/topology/fold/check_end.rs` | 4 |
| `src/topology/fold/check_integration.rs` | 12 |
| `src/topology/fold/outcome.rs` | 12 |
| `src/topology/fold/parse.rs` | 1 |
| `src/topology/fold/predicates.rs` | 28 |
| `src/topology/fold/region.rs` | 8 |
| `src/topology/fold/start.rs` | 5 |

## 2. The root is a pure subsequence of the base

Removing the inserted block — the ten `mod` declarations at lines 46–55
plus the adjacent blank — and diffing against `1cbdccd:src/topology/fold.rs`:

| measure | value |
|---|---|
| hunks | **4**, all deletions |
| lines added | **0** |
| lines deleted | **3394** |
| base lines / retained lines | 4245 / 851 |
| sha256 of the retained root | `37e09571e331bacad29b640462301ad0bead03599983c57c9b34463f9a2ed48a` |

Zero added and zero modified lines is the claim: the root's retained content is
the base file with whole ranges removed and nothing else. Fewer hunks than moved
ranges is expected — adjacent moved ranges coalesce. Reproduce the digest from
the head rather than from the range list:

    git show <head>:src/topology/fold.rs | sed '46,56d' | sha256sum

## 3. Visibility: preserved, not widened

**Unrestricted public items** — the crate's actual API surface — per file at head:

| file | `^\s*pub ` lines |
|---|---|
| `fold.rs` | 42 |
| `apply.rs` | 0 |
| `check_attempt.rs` | 0 |
| `check_candidate.rs` | 0 |
| `check_end.rs` | 0 |
| `check_integration.rs` | 0 |
| `outcome.rs` | 0 |
| `parse.rs` | 1 |
| `predicates.rs` | 28 |
| `region.rs` | 0 |
| `start.rs` | 1 |
| **head total** | **72** |
| **base `src/topology/fold.rs`** | **72** |

Conserved: **yes**. And identical as
multisets, character for character: **yes**.

A wider expression does **not** conserve, and saying so is the point: `^\s*pub(\(| )`
also matches every added `pub(super)`, so it grows by exactly the number of
preservation annotations. That expression appeared in an earlier revision of the
body as evidence for this claim; it made a true conclusion rest on a false
measurement, and the reviewer was right to reject it.

**Preservation annotations** — `pub(super)` per file, and whether each annotated
item was private at the base:

| file | `pub(super)` lines |
|---|---|
| `apply.rs` | 23 |
| `check_attempt.rs` | 12 |
| `check_candidate.rs` | 2 |
| `check_end.rs` | 4 |
| `check_integration.rs` | 12 |
| `outcome.rs` | 12 |
| `region.rs` | 7 |
| `start.rs` | 4 |
| **total** | **76** |

Named items checked individually: **76**; those that were **not**
private at the base — which would be **widening**: **0** .
Items unrestricted-`pub` at the base and no longer so — which would be
**narrowing**, forbidden in the other direction: **0** .

**Root retention is a means, not the requirement.** The requirement is no
visibility widening, no glob re-exports, and external paths preserved. Measured
against those three directly, so the split is judged by its effect and not by its
shape:

| property | measurement |
|---|---|
| no widening | **0** of 76 annotated items were public at the base |
| no narrowing | **0** of 72 base public items lost their visibility |
| no glob re-export | `pub use` lines anywhere in the module: **0** — so neither a glob nor a named one |
| external paths preserved | **10** distinct `topology::fold::<item>` paths are named from outside the module across 16 files; those no longer resolving to a public root item: **0**  |

The external-path check is the one that catches a split satisfying the first three
by accident: it enumerates what the rest of the crate actually names and confirms
each name is still a public item of `topology::fold`.

`pub(super)` in `fold::child` means visible in `fold`, and an item visible in a
module is visible in its descendants — so the effective set is `fold` plus its
descendants, which is what private-in-`fold` already meant.

## 4. Lint prologues, by walking the attributes rather than counting them

**File-level inner attributes** (`#![…]`), which is the form a child would
inherit a blanket allow through:

| file | inner attributes |
|---|---|
| `1cbdccd:src/topology/fold.rs` | none |
| `src/topology/fold.rs` | none |
| `src/topology/fold/apply.rs` | none |
| `src/topology/fold/check_attempt.rs` | none |
| `src/topology/fold/check_candidate.rs` | none |
| `src/topology/fold/check_end.rs` | none |
| `src/topology/fold/check_integration.rs` | none |
| `src/topology/fold/outcome.rs` | none |
| `src/topology/fold/parse.rs` | none |
| `src/topology/fold/predicates.rs` | none |
| `src/topology/fold/region.rs` | none |
| `src/topology/fold/start.rs` | none |

**Every outer attribute naming a lint, with the item it is attached to:**

| where | attribute | attached to |
|---|---|---|
| `1cbdccd:src/topology/fold.rs:1177` | `#[allow(clippy::too_many_lines)]` | `fn check_started_run(` |
| `1cbdccd:src/topology/fold.rs:3532` | `#[allow(clippy::too_many_lines)]` | `fn apply(&mut self, body: &TopologyEventBody, derived: &Derived) {` |
| `src/topology/fold/apply.rs:19` | `#[allow(clippy::too_many_lines)]` | `pub(super) fn apply(&mut self, body: &TopologyEventBody, derived: &Der` |
| `src/topology/fold/start.rs:73` | `#[allow(clippy::too_many_lines)]` | `pub(super) fn check_started_run(` |

No file-level attribute exists on either side, so no child inherits a blanket
allow — the failure #100 made and #102 then made independently. Each allow that
does exist is item-level and is attached to the same function at the head as at
the base.

## 5. Module shape

**Cross-child imports**, enumerated:

- `check_attempt.rs:4` — `use super::region::{`
- `check_attempt.rs:7` — `use super::start::check_ladder;`
- `check_end.rs:3` — `use super::start::outcome_name;`
- `check_integration.rs:4` — `use super::region::{GitRefName, ineligible_detail, ordinal};`

4 edges from 3 importing children, targeting ['region', 'start'].
Neither target imports a sibling, so the graph is one-directional and acyclic,
and these are the only sibling references in the module.

**Last items of the root**, so test-items-last (§12) is checked rather than
asserted: [(861, '#[cfg(test)]'), (862, 'mod tests;')]

**`pub use` re-exports anywhere in the module**: **0**. No path is rescued by a re-export.

## 6. The measuring run at this head

**The eight commands**, `w1-eight-iso`, run by the steward:

```
eight-command baseline at fc55ee8 in /srv/worktrees/w2-m1, 2026-09-03T15:43:43Z
PASS 01 fmt                 1s
PASS 02 clippy             16s
PASS 03 test               55s
PASS 04 msrv               10s
PASS 05 release-record      0s
PASS 06 pr-policy           0s
PASS 07 pr-ledger           0s
PASS 08 docs-consistency    0s
ALL 8 PASS at fc55ee8
```

Environment, recorded because it has decided outcomes on this box: a plain child
of the steward's session shell — **not** `nohup`-detached and **not** inside
`tmux`. Two `tmux` runs of these same eight commands wedged the `agent::proc`
terminal family indefinitely while plain-shell runs of the same heads finished in
under a minute. This shell has no controlling terminal, which is a third
environment from either, and it is the one every green W2 gate run has used.

**Test inventory** at this head: **1848** entries, sha256 `6fe9f0d53113ecdcf980756c44f206b94b5b0a3652eb2b1696e3e499bf4441c0`.
Base listing: **1848** entries, sha256 `6fe9f0d53113ecdcf980756c44f206b94b5b0a3652eb2b1696e3e499bf4441c0`. Compared **name by name**, the two listings are **identical** — zero names added, removed or renamed.

This is a **Linux** figure and no Windows figure is asserted: `--list` has to run
the test binary and the Windows one cannot be run on this box. What it proves is
that the normalised multiset of test names is unchanged — not that any command's
raw output is byte-identical.

**Broken intra-doc links, `--document-private-items`** — the only setting that can see this change, because every child is a private `mod` and a plain run does not document private items:

| measure | base | head |
|---|---|---|
| warnings | 76 | 76 |
| message multiset | identical | |

Locations only at base: ['src/topology/fold.rs:2558:66', 'src/topology/fold.rs:4203:14', 'src/topology/fold.rs:4217:7']

Locations only at head: ['src/topology/fold/check_integration.rs:91:66', 'src/topology/fold/region.rs:68:14', 'src/topology/fold/region.rs:82:7']

The difference is relocation, not repair: the same messages, at the child files that now hold those doc comments.

**Rule 7h — the Windows `-D warnings` check.** At this head: **0** lines beginning `error`, `Finished`.
**The control**, an ungated unused import planted in a disposable copy outside the worktree: `error: unused import: `std::collections::BTreeMap as ProbeUnused``. A bare rc=0 says *the input is clean* and *the detector is blind* equally well; the control is what separates them.

## 7. The census's depth, bound by a mutation planted in a GRANDCHILD

Pass 1's finding was that the census claimed the fold module's production region
and scanned one directory level of it. The repair widens the walk. **What binds a
depth claim is a mutation OUTSIDE the old boundary**: the earlier recheck planted
its third charge in `check_candidate.rs`, a **direct** child, and so confirmed
only that the census fires — a control inside a truncated domain cannot detect
the truncation.

Planted by the steward at this head: `mod debit;` in `fold/apply.rs`, a
`pub(super)` helper in **`fold/apply/debit.rs`** — a **grandchild** — calling
`self.charge_allowance(...)`, and a call to it immediately after the existing
charge in `apply_settlement`, so every retained failure is charged twice.

| check | result |
|---|---|
| census on the unmutated tree | **passes** (binary reused from the gate run at this same head in this same private base) |
| census with the grandchild charge | **fails** — assertion `left == right` failed: `charge_allowance` is called 3 time(s) in the fold module's production region; schema 4 has two settlement appliers and each must charge the rung — a failure through `apply_settlement` and a success through `apply_candidate_prepared`; left: 3; right: 2 |
| the mutated run built from this tree | **yes** |
| test outcome | **FAILED, which is the intended result** |

Reverted afterwards, and the revert checked against `git show HEAD:…` rather than
against a copy of the checker's own making: `src/topology/fold/apply.rs` hashes
back to its committed content and `git status --untracked-files=all` is empty, so
the probe left nothing behind.

**Why the "built from this tree" row is there.** `upstroke-build` puts arms in one
slot and cargo's `-C metadata` hash is identical across source paths, so a probe
can be served another arm's binary and report `ok` in 0.02s with no compile line
— green, and vacuous. Every probe result here is paired with the line naming the
path it was built from. The guard also has to know the verb: `cargo check` prints
**Checking** and `cargo rustdoc` prints **Documenting**, not `Compiling`, and a
guard matching only `Compiling` reports a good run as vacuous.

## 8. Rule 7g — the prose a split falsifies, and rule 9's three-state trace

The window, so it can be read rather than inferred:

    git grep -nP 'fold\.rs|topology::fold|\bfold::' -- src '*.md' \
      ':!src/topology/fold.rs' ':!src/topology/fold/' \
      ':!reviews/' ':!decisions/' ':!proposals/'

| measure | value |
|---|---|
| hits | **35** |
| of which are `scaffold.rs` matching the substring `fold.rs` | **6** |
| markdown hits outside the immutable directories | **0** |

Every remaining hit names `crate::topology::fold::TopologyFold` or a method on
it, and every type and method path is unchanged — an inherent `pub fn` on a `pub`
type stays publicly reachable whichever private child holds its `impl`. The two
that named a **path** were the census repoints this change makes.

**A categorical claim has to be traced across BASE, POST-SPLIT and
POST-REPAIR**, because a split can change a sentence's truth value by changing
what its referent denotes — a false claim can become vacuous, and repairing the
referent can make it false again. Checking only the head cannot see that.

The domain, derived at this head: comment lines in the ten children carrying
universal-quantifier or file-scope language — **35** lines, of which
**7** name a scope word the split could have moved:

| site | claim |
|---|---|
| `apply.rs:13` | Total by construction: every lookup here was proved to succeed by the |
| `apply.rs:265` | **The single write, and both settlements reach it through here.** The |
| `check_attempt.rs:597` | more: `Retained` carries no transition, so nothing here makes |
| `predicates.rs:114` | and leak here. So every predicate below is false once poisoned. |
| `predicates.rs:300` | convenient way to obtain what the fold will accept rather than the only |
| `region.rs:73` | other kind.** This took a `survives: bool`, and exactly one caller ever |
| `region.rs:76` | settlement this fold accepts — `candidate_prepared` is the sole successful |

Each was traced from the base to this head, and **none is falsified**:

- **`predicates.rs`, "so every predicate below is false once poisoned" — the one
  whose referent genuinely moved.** At the base, "below" denoted positionally the
  twelve readers **plus** `plan_transition`, `apply_delta` and `derived_outcome`,
  which followed it in the same `impl` block. At the head it denotes exactly the
  twelve readers: the transition pair stayed in the root and `derived_outcome`
  went to `start.rs`. The sentence says every **predicate** below, and those three
  are not predicates — the pair is the mutation path, and `derived_outcome`
  returns an outcome — so the narrowing makes scope and subject **coincide** where
  the positional scope previously over-reached. True at the base, true at the
  head, and more tightly bounded at the head. Its four named exceptions —
  `pipeline_held`, `run_is_ending`, `backoff_pending`, `questions_open` — all
  still follow it in the same file.
- The rest are function-local, arm-local or module-wide, and their referents
  travelled with them: the `apply` doc's "every lookup **here**" and "nothing in
  **this function**" are scoped to `apply`; `charge_allowance`'s "both
  settlements reach it through **here**" is scoped to that function;
  `check_attempt.rs`'s "nothing **here** makes the generation terminal" is scoped
  to the `Retained` match arm it sits inside, which moved whole; `region.rs`'s
  "this **fold**" still denotes the whole module; its "exactly one **caller**" is
  a claim about call sites, which this change does not touch; and
  `predicates.rs:300`'s "the only convenient way" is not a scope claim at all.

**No referent was repaired in this pull request**, so the third state is the
second: there is no post-repair sentence whose scope fix could have reintroduced
a falsehood. That is stated rather than omitted, because "we did not need the
third state" and "we did not look at it" are not the same claim.
