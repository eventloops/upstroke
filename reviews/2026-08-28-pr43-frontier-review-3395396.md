# PR #43 — frontier review record, `3395396`

| field | value |
|---|---|
| **Verdict** | **CHANGES_REQUIRED, five findings** |
| **Reviewed SHA** | `3395396c1838156545890e64c1d2516f744966e6` |
| Pull request | eventloops/upstroke#43, into `codex/parallelism-design` |
| Reviewer | `gpt-5.6-sol`, `model_reasoning_effort=max`, read-only sandbox |
| Transport | `codex exec` via `~/bin/review-pr.sh 43`, 90-minute per-pass limit |
| Driver knobs | `REVIEW_ROOT` (a worktree at the reviewed sha), `REVIEW_PROMPT_EXTRA` |
| CI at the reviewed SHA | GREEN on the newest run per name, selected by run id |
| Inputs | the diff, the tree at that sha, both committed prior review records, the preserved CI logs, and the pull request's title and body |

The third review of #43. Its first finding is the shape this branch keeps producing: a
matching rule was widened where the previous reviewer pointed, and the sentence stating the
narrower rule was left standing beside it, so the record carried both and they
contradicted. Its third is sharper still — the correction to a wrong count was itself a
wrong count, arrived at by a `grep -c` that counted a doc-comment mention alongside the
calls.

Its fifth finding is the one that generalises: the structural deletion of recomputable
facts had been applied to digits and not to number-words, so *"two records"*, *"narrowed
four times"* and *"the second harness fault"* all survived a rule written to remove exactly
them.

## The review, verbatim

## Findings

1. **P1 — the macOS matching rule still permits the exact misclassification the previous review identified.** The record first says a matching red “is this failure until shown otherwise,” then says the opposite: it is not automatically this failure and cleanup must be ruled out ([macOS record](/srv/worktrees/pr43-r3/reviews/2026-08-28-macos-proc-signal-single-failure.md:92)). Concrete sequence: reaper cleanup regresses → it writes SIGTERM to `PENDING_TERMINATION` → monitor exits 143 → test/site/status match → the first bold rule classifies it as the known observation rather than a regression. The later contradictory rule does not provide an unambiguous oracle. Consequently the body’s width claim and the ledger’s `fixed` disposition are too strong.

2. **P2 — the previous scope finding remains unfixed in its sibling document.** The exact diff adds four Markdown files, but the Windows record still says “this diff … adds two files” ([Windows record](/srv/worktrees/pr43-r3/reviews/2026-08-28-windows-topology-kill-single-failure.md:142)). That describes neither the historical `02b7399` diff, which the record itself says added one file, nor this head. The new ledger row acknowledges that both the body and a record were stale, but its disposition repairs only the body ([pr.md](/srv/worktrees/pr43-r3/pr.md:137)). This leaves the change’s durable record advertising a narrower scope and violates the no-silent-scope-widening rule.

3. **P2 — the `_exit(127)` repair replaces the reviewed fact with another false count and drops it from the ledger.** The previous review correctly identified three literal `_exit(127)` calls. The repair says there are four ([macOS record](/srv/worktrees/pr43-r3/reviews/2026-08-28-macos-proc-signal-single-failure.md:82)); the source has exactly three, at lines 4387, 4397, and 4417 ([proc.rs](/srv/worktrees/pr43-r3/src/agent/proc.rs:4387)). Neither new mechanism row records this part of the prior finding, so the original defect disappears while its attempted correction is itself wrong.

4. **P2 — the newly added ledger rows do not faithfully record the previous review.**

   - The scope row says the reviewed body claimed the macOS record was “modified once”; it actually claimed “modified twice,” while history shows an addition followed by three modifications. The committed prior review states that exact defect ([prior review](/srv/worktrees/pr43-r3/reviews/2026-08-28-pr43-frontier-review-be76254.md:37)).
   - Its disposition says the body has “no chronology at all,” contradicted by “narrowed four times” ([pr.md](/srv/worktrees/pr43-r3/pr.md:17)), and says a revert command is generated although the current body contains no command.
   - The mechanism and fallback findings were introduced by the `83d5560` repair of the earlier mechanism finding, yet both are marked `introduced_by_feature` rather than `fix_regression` despite carrying the prior ID ([pr.md](/srv/worktrees/pr43-r3/pr.md:134)). That contradicts the ledger’s recurrence definition ([FINDINGS.md](/srv/worktrees/pr43-r3/reviews/FINDINGS.md:75)) and hides that these were repair regressions.

5. **P2 — the claimed structural deletion was not performed consistently.** The body says every recomputable fact was deleted ([pr.md](/srv/worktrees/pr43-r3/pr.md:31)), but outside the ledger it still contains counts and mutable state: “two records,” “narrowed four times,” another pull request’s current ledger state, sibling CI conclusions “earlier the same day,” exact-head local gate conclusions, and a “second” harness fault with serial pass conclusions. A subsequent commit immediately makes “every gate … passes locally at this head” unsupported unless every gate is rerun, reproducing the stale-body failure this round was meant to eliminate.

I found no necessary information lost solely through deletion: fingerprints and retrieval commands remain in the records, while paths, commits, and rollback range are recoverable from the diff and Git history. The exact diff otherwise checks out as review-only Markdown; no `src/**`, `DESIGN.md`, decision, gate, or configuration file changed, so no new panic, `anyhow`, or path-handling violation was introduced.

VERDICT: CHANGES_REQUIRED