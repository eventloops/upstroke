# Frontier invalidation canary

Disposable content used only to prove that a pull-request body edit invalidates the same App-owned frontier-review check after PR #14's workflow rollout. This branch must never merge.
